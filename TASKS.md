# TASKS.md — 実行計画

仕様は [SPEC.md](SPEC.md)、実装ルールは [CLAUDE.md](CLAUDE.md) を参照。

## この文書の使い方

- **上から順に進める。** 依存が明記されているタスクは、依存先が完了してから着手する
- 1 タスク = 1 セッションで完結する粒度に切ってある
- **完了条件(DoD)を全て満たしてから次へ進む。** 一つでも欠けていればそのタスクは未完了
- 完了したら `- [ ]` を `- [x]` に変え、コミットする

## 進捗

| フェーズ | タスク数 | 状態 |
|---|---|---|
| Phase 0 基盤 | 4 | 未着手 |
| Phase 1 第1リリース | 15 | 未着手 |
| Phase 2 拡張 | 6 | 未着手 |

## 第1リリースの全体像

第1リリースは重い。**T-18 のデモメディア立ち上げを最後まで残さず、T-12(応募)が通った時点で一度データを入れて動かす**こと。自分がヘビーユーザーになるのが、仕様のズレを見つける唯一の方法(→ [SPEC.md 16.1](SPEC.md#161-第1リリースが重い))。

---

# Phase 0 — 基盤

## - [~] T-01 開発環境の構築と本番環境の実測 — **ローカル完了 / 本番調査が残**

**目的**: ローカル環境を作り、**本番(エックスサーバー)の実効値を調べて設計の前提を確定する**

### 完了した内容

- [x] git リポジトリを初期化(親の `c:\Users\hismi` が別リポジトリだったため独立させた)
- [x] Laravel **13.8** を導入
- [x] PHP を **8.4** に決定(→ [SPEC.md 13.3](SPEC.md#133-php-バージョンの選定))
- [x] `composer.json` の `config.platform.php` を `8.4.0` に固定(依存解決を本番に合わせる)
- [x] `docker-compose.yml`(app / mysql / mailpit / node)と `docker/php/Dockerfile`
      - 本番と同じ **Apache + PHP 8.4** 構成にし、`public` をドキュメントルートにした
      - 拡張は Xserver にあるものだけ(pdo_mysql / mbstring / gd / zip / bcmath / exif)
- [x] Tailwind CSS 4 + Vite 8(Laravel 13 に標準同梱)
- [x] `.gitignore` から `public/build` を除外(本番でビルドできないため成果物を配布する)
- [x] `.env.example` を整備。**Redis の設定項目を削除**し、session / queue / cache を `database` に固定
- [x] `config/app.php` の `timezone` を `Asia/Tokyo` に変更
- [x] `phpunit.xml` のテスト DB を sqlite → **MySQL(`kyujin_testing`)** に変更(→ [SPEC.md 13.4](SPEC.md#134-テストは本番と同じ-mysql-で走らせる))
- [x] `README.md` に起動手順と本番との差異を記載
- [x] `docs/Xserver実機調査.md` に調査手順を用意

**検証済み**
- `docker compose up -d --build` → http://localhost:8080 が 200 で表示される
- `php artisan migrate` が MySQL に対して成功
- `php artisan test` が MySQL に対して成功(2 passed)
- `npm run build` で `public/build` が生成され、git の追跡対象になっている
- `vendor` / `node_modules` / `.env` は除外されている

### 残っている作業(要: エックスサーバーの契約と SSH アクセス)

- [ ] `docs/Xserver実機調査.md` の手順を実行する
- [ ] **サーバーパネルで PHP を 8.4 に切り替える**(新規ドメインの初期値は 8.3)
- [ ] 調査結果を SPEC.md 13.1 の「要確認事項」に反映し、確定した値に書き換える
- [ ] 実測値に合わせて `docker/php/php.ini` を調整する

> **この調査結果は T-18(デプロイ)の方針を左右する。**
> git が使えなければ rsync ベースに、`symlink()` が使えなければ `public_html` の構成を変更することになる。
> **ただし T-02 以降の実装は調査を待たずに進められる。**

---

## - [x] T-02 介護特化マスタとシーダー — **完了**

**目的**: 業界特化の実体を用意する。**汎用パッケージとの差はここに集約される**

**依存**: T-01

### 実装した内容

- [x] マイグレーション 7 テーブル: `prefectures` / `cities` / `qualifications` / `facility_types` / `job_categories` / `employment_types` / `job_features`
- [x] 全マスタに `code`(製品が定める安定した識別子)・`is_enabled`・`sort_order`
- [x] `qualifications` / `facility_types` / `job_features` にカテゴリ列(画面でグループ表示するため)
- [x] `facility_types` に `short_name`(特養・老健・サ高住など現場の呼び方。検索のヒット率が上がる)
- [x] `app/Models/Concerns/IsMaster.php` — `enabled()` / `ordered()` / **`selectable()`** スコープ
- [x] `database/seeders/MasterSeeder.php` — 冪等な `sync()`。**顧客が管理する項目を上書きしない**
- [x] 各マスタのシーダーと `DatabaseSeeder`
- [x] `masters:import-cities` コマンド(総務省の全国地方公共団体コード CSV から全件取り込み)

### 投入件数(SPEC.md 11章と一致)

| マスタ | 件数 |
|---|---|
| 都道府県 | 47 |
| 市区町村 | 74(主要のみ。→ 下記の残作業) |
| 保有資格 | 22 |
| 施設形態 | 27 |
| 職種 | 12 |
| 雇用形態 | 6 |
| こだわり条件 | 20 |

### 検証済み(`tests/Feature/MasterDataTest.php` 9件 / 33 assertions)

- 全マスタが仕様どおりの件数で投入される
- 介護領域の主要マスタが漏れなく入っている / 略称が正しく返る
- 都道府県が 8 地方すべてをカバーしている
- **無効にしたマスタが選択肢に出てこない**
- 選択肢が顧客の指定した並び順で返る
- **シーダーを再実行しても行が重複しない**
- **シーダーを再実行しても顧客が調整した `is_enabled` / `sort_order` が消えない** ← 最重要
- シーダーを再実行すると製品が管理する項目(name / category)は最新に戻る
- 市区町村が都道府県に正しく紐づく

### 仕様変更

- **`workplaces.city`(文字列)→ `city_id`(マスタ参照)** に変更した。
  文字列では表記ゆれで市区町村の絞り込み検索が成立しないため。SPEC.md 8.2 を更新済み。
- SPEC.md に **11.0 マスタの所有権**(製品が管理する項目 / 顧客が管理する項目)と **11.7 市区町村マスタの網羅性** を追加。
- CLAUDE.md に絶対ルール **3.7 マスタの `is_enabled` / `sort_order` を製品側で書き換えない** を追加(以降の番号を繰り下げ)。

### 追記: 市区町村を全国 1,747 件に拡張(完了)

主要 74 件のみだった市区町村マスタを、総務省「全国地方公共団体コード」の公式データ(xlsx)から
**全国 1,747 件**に拡張した。

- `masters:build-city-data` コマンドで xlsx → `database/data/cities.php`(PHP 配列)を生成
  - xlsx は ZIP 内の XML を直接パースして読む。この 1 箇所のためだけに `phpspreadsheet` を入れていない
  - 半角カタカナのふりがなを `mb_convert_kana(..., 'KV')` → `mb_convert_kana(..., 'c')` の
    **2 段階**でひらがなに変換する(1 回の呼び出しでは連鎖しないため)
- `CitySeeder` はこのデータファイルを読むだけになり、**顧客環境でも `db:seed` だけで全件入る**
  (手作業の取り込みは不要になった)
- `MasterSeeder::sync()` を **一括処理に書き換えた**。1,747 件を 1 行ずつ `firstOrNew` + `save` すると
  3,500 クエリになり、本番(共有レンタル)の実行時間上限で deploy が止まる。
  既存行を 1 回で読み、新規はまとめて `insert`、更新は差分があるものだけに絞った(214ms で完了)
- 政令指定都市の行政区(札幌市中央区など)は公式データに無く、このマスタにも含まれない。
  検索は市区町村単位までになる
- 市区町村が合併・分割されたら、公式ファイルを再取得してコマンドを再実行する

---

## - [x] T-03 サイト設定とテーマ機構 ★製品の生命線 — **完了**

**目的**: 「コードは1本」と「デザインは個別制作」を両立させる仕組みを作る

**依存**: T-01

### 実装した内容

- [x] `site_settings` テーブル(1行のみ)。メディア情報・見た目・機能のオンオフ・販売プランの上限・規約
- [x] `App\Models\SiteSetting::current()` — リクエスト内メモ化。行が無ければ既定値で作る
- [x] `App\Support\Theme\ThemeManager` — ビューの探索順を **テーマ → コア → resources/views** に設定
- [x] `theme()` / `theme_asset()` ヘルパー(`app/Support/helpers.php` を composer で autoload)
- [x] `ThemeServiceProvider` — 起動時にパスを適用し、全ビューに `$site` を渡す
- [x] `resources/views/core/` にレイアウト・ヘッダー・フッター・トップページ
- [x] `resources/views/themes/default/` を標準テーマとして用意(コアをそのまま使うため Blade は 0 個)
- [x] `public/themes/default/css/theme.css`
- [x] トップページのルートとコントローラ(Laravel の welcome を置き換え)

### 検証済み(`tests/Feature/ThemeTest.php` 8件)

- テーマに何も置かなければコアが使われる
- **テーマに同名のファイルを置くとそちらが使われる**
- **テーマに無いファイルはコアにフォールバックする**(トップだけ差し替えてもレイアウトはコア)
- テーマを切り替えても全ページが 200 で表示される
- **存在しないテーマが設定されていても標準テーマで表示される**(サイトが真っ白にならない)
- テーマのアセット URL が返る / 無ければ標準テーマにフォールバックする
- 導入済みテーマを列挙できる

### 設計上の判断

- **サイト設定をキャッシュに載せない。** 本番のキャッシュストアは database(Redis が無い)なので、
  1 行のテーブルをキャッシュしても DB クエリが DB クエリに変わるだけで速くならない。
  さらに Eloquent モデルをキャッシュに入れると復元に失敗して `__PHP_Incomplete_Class` になる事故が起きた。
  リクエスト内メモ化のみにした。
- **テーマの CSS はビルドしない。** 本番に Node.js が無いため、テーマ固有の CSS は素の CSS にして
  `public/themes/{theme}/` に置く。デザイン制作者がファイルを差し替えるだけで済む(SPEC.md 9.2.1)。
- **テーマの「導入済み」判定はビューディレクトリの有無。** 設定されたテーマが無ければ標準テーマに落とす。
- `$site` をコアが全ビューに渡すことで、テーマがモデルを触らずに済むようにした(CLAUDE.md 3.2)。

### 残作業

- [ ] 運営者のサイト設定画面(テーマ選択・機能のオンオフ)は **T-16** で作る。
      現時点では設定値を直接変更することでテーマを切り替えられる。

---

## - [x] T-04 3種類の認証 — **完了**

**目的**: 運営者 / 掲載企業 / 求職者のログインを分離する

**依存**: T-01

### 実装した内容

- [x] Laravel 既定の `users` テーブルとモデルを削除(3 種類のユーザーを 1 テーブルで表現できないため)
- [x] `admin_users` / `companies` / `company_users` / `job_seekers` のマイグレーション
- [x] `config/auth.php` に 3 ガード(`admin` / `company` / `seeker`)と 3 ブローカー
- [x] `BaseLoginController` / `BasePasswordResetController` — 3 ガードで共通処理を 1 箇所に
- [x] 各ガードのログイン・ログアウト・パスワード再設定(計 22 ルート)
- [x] ルートファイルを文脈ごとに分割(`routes/web.php` / `admin.php` / `company.php`)
- [x] 未認証時に**その画面に対応するログイン画面へ**送るリダイレクト(`bootstrap/app.php`)
- [x] ログイン試行の回数制限(5回/60秒、ガード+メール+IP 単位)
- [x] `is_active = false` のアカウントはログイン不可
- [x] 認証画面は `core/` に置き、テーマで上書き可能。フォームは共通コンポーネント化
- [x] ダッシュボード(運営者 / 掲載企業)とマイページ(求職者)の雛形
- [x] ファクトリ 4 種

### 検証済み(21 テスト / 73 assertions)

**認証分離**(`AuthenticationSeparationTest`)
- 未ログインでは各管理画面に入れず、**それぞれ対応するログイン画面へ**送られる
- **掲載企業の担当者は運営管理画面に入れない**
- **求職者は掲載企業の管理画面にも運営管理画面にも入れない**
- 運営者も掲載企業の管理画面には入れない(なりすましは Phase 2 の別機能)
- 3 つのガードに同時にログインできる

**ログイン**(`LoginTest`)
- 3 ガードそれぞれでログイン・ログアウトできる
- **別のガードの認証情報ではログインできない**
- 無効化されたアカウントはログインできない
- **ログアウトしても他のガードのログイン状態は維持される**
- 試行回数の上限を超えるとロックされる

**パスワード再設定**(`PasswordResetTest`)
- 再設定が通る / 不正なトークンでは通らない
- **同じメールアドレスの運営者と求職者が互いの再設定を壊さない**
- 登録の有無にかかわらず同じ応答を返す(登録者の存在を漏らさない)

### 設計上の判断

- **パスワード再設定のトークンを種別ごとに別テーブルにした。** Laravel 既定のテーブルはメールが主キーで、
  同じメールを持つ運営者と求職者が同時に申請すると互いのトークンを上書きしてしまう。
- **パスワード再設定メールはキューに載せない。** 本番のキューは cron で毎分実行のため最大 1 分遅れる。
  再設定は即時性が重要と判断した。他の通知は T-15 でキュー経由にする。
- **ログアウトで `session()->invalidate()` を使わない。** セッション全体が消えて他のガードまでログアウトするため、
  `regenerate()` でセッション ID だけ振り直す。
- ログイン失敗時は原因を問わず同じ文言を返す(アカウントの有無を推測させない)。
- `companies` テーブルは T-05 の対象だが、`company_users` が参照するためここで作成した。

### スコープ外(後続タスク)

- 掲載企業と求職者の**新規登録**: T-05(運営者が掲載企業を登録)/ T-11(求職者の会員登録)
- 運営者アカウントの管理画面: T-16

---

# Phase 1 — 第1リリース

## - [x] T-05 掲載企業の管理 — **完了**

**目的**: 運営者が掲載企業を登録し、担当者アカウントを発行できるようにする

**依存**: T-04

### 実装した内容

- [x] 運営者の掲載企業 CRUD(一覧・検索・ステータス絞り込み・登録・詳細・編集)
- [x] **担当者アカウントの発行と招待メール。パスワードはメールで送らず本人に設定させる**
- [x] 招待メールの再送、担当者の有効・無効切り替え
- [x] 掲載企業側の自社情報編集(`/company/profile`)
- [x] ロゴのアップロード(EXIF 補正 → 縮小 → WebP 変換)
- [x] `ImageUploadService` — 画像保存の共通処理
- [x] `uploads` ディスク(`public/uploads`)を追加
- [x] FormRequest 3 種。都道府県と市区町村の整合も検証
- [x] フォーム部品の共通コンポーネント(`x-form.field` / `input` / `select` / `textarea`)

### 検証済み(17 テスト / 42 assertions)

**運営者**(`CompanyManagementTest`)
- 掲載企業を登録できる / 企業名が無ければ登録できない
- 市区町村が都道府県と一致しないと登録できない
- ロゴが WebP に変換される / 差し替えると古いファイルが消える
- 担当者を追加すると招待メールが送られる
- **招待時にリクエストへパスワードを混ぜても設定されない**
- 同じメールアドレスの担当者は登録できない
- **他社の担当者を URL で指定しても操作できない(404)**

**掲載企業**(`CompanyProfileTest`)
- 自社の企業情報を編集できる / 編集画面に他社の情報は出ない
- **リクエストに他社の ID を混ぜても他社は書き換わらない**
- **掲載企業はステータスを変更できない**(掲載停止を自分で解除できてはいけない)

### 設計上の判断

- **掲載企業の画面は URL に企業 ID を含めない。** 対象は常にログイン中の担当者の所属企業。
  ID を受け取らなければ他社を指定する余地が構造的に無くなる(CLAUDE.md 3.8)。
- **招待メールにパスワードを書かない。** メールは平文で残り続けるため、
  パスワード再設定と同じトークンの仕組みで本人に設定させる。
- **掲載企業の削除を提供しない。** 求人と応募が紐づくため、ステータスを `archived` にして止める。
- **画像は `public` ディスクではなく `uploads` ディスクへ。** `public` は `storage:link` の
  シンボリックリンク前提で、本番で使えないと画像が全て表示されなくなる。
- 画像処理は **GD ドライバに固定**。Imagick は本番での利用可否が環境依存のため。

### 途中で見つかった問題

**Docker の GD が WebP 非対応でビルドされていて画像保存が落ちた。**
`libwebp-dev` と `--with-webp` が抜けていたのが原因。Dockerfile を修正済み。
同じことが本番でも起こりうるため、`docs/Xserver実機調査.md` に
**GD の WebP 対応**と **EXIF** の確認手順を追加した。未対応なら保存形式を JPEG に変える必要がある。

---

## - [x] T-06 事業所・勤務地の管理 — **完了**

**目的**: 求人の前提となる事業所を登録できるようにする

**依存**: T-05, T-02(市区町村全件・完了済み)

### 実装した内容

- [x] `workplaces` マイグレーション。住所は `prefecture_id` + `city_id`(マスタ参照)+ `address`(番地以降)
- [x] 運営者の代行編集(`admin/companies/{company}/workplaces`)と掲載企業の自社編集(`company/workplaces`)
- [x] `ManagesWorkplaces` トレイトで CRUD ロジックを共有。実処理は `do*` メソッドに切り出し、
      各コントローラの公開アクションは URL のパラメータ順に合わせて定義
- [x] 施設形態・都道府県・市区町村を `selectable()` 経由で選択肢化
- [x] 事業所写真のアップロード(`ImageUploadService` を再利用)
- [x] 求人が紐づく事業所は削除できないガード(`Workplace::hasJobPostings()`。T-07 まで暫定実装)
- [x] フォーム部品の共通化(`x-workplace-form-fields`)、都道府県セレクトの補助 JS を共通化

### 検証済み(15 テスト / 35 assertions)

**運営者**(`Admin\WorkplaceManagementTest`)
- 任意の企業に事業所を代行登録できる
- **他社の事業所を URL で組み替えても編集できない(404)**
- 一覧はその企業の事業所だけを表示する
- 掲載企業は運営管理画面の代行登録ルートに入れない

**掲載企業**(`Company\WorkplaceManagementTest`)
- 自社の事業所を登録・削除できる
- **リクエストに他社の `company_id` を混ぜても他社の事業所にはならない**
- **他社の事業所を編集・更新・削除できない(404)**
- 一覧には自社の事業所だけが表示される
- 写真が WebP に変換される
- 無効化されたマスタが選択肢に出ない
- 市区町村が都道府県と一致しないと保存できない
- `job_postings` テーブルが無い間は求人なし扱いで削除できる

### 途中で見つかった重大な落とし穴

**同じ CRUD トレイトを運営者(`{company}/workplaces/{workplace}`)と掲載企業(`workplaces/{workplace}` のみ)で共有しようとして、暗黙のルートモデルバインディングが位置ズレを起こした。**

`edit(Workplace $workplace, ?Company $company)` のように、トレイトの公開メソッドに固定の引数順を持たせてルートに直接晒したところ、実行時に
`Argument #1 ($workplace) must be of type Workplace, Company given` で落ちた。

原因: Laravel の暗黙バインディングは、型が一致する引数がある場合、**ルートパラメータの URL 登録順**でメソッド引数に位置合わせして渡す。運営者側 URL は `company → workplace` の順で登録されるため、メソッド側の引数順(`workplace → company`)と逆になり、型は合っていても位置がずれて TypeError になった。

**対策**: 公開アクション(`index`/`edit` など)は**各コントローラで URL のパラメータ順に合わせて個別に定義**し、実処理だけをトレイトの `protected do*` メソッド(固定引数順でよい)に委譲する形に変更した。CLAUDE.md 3.9 にルール化した。

### 設計上の判断

- 求人テーブルがまだ無いため、削除ガードは `Schema::hasTable('job_postings')` を使った暫定実装にした。
  T-07 で実装したら通常のリレーション判定に置き換えること(`Workplace::hasJobPostings()` のコメントを参照)。
- 都道府県→市区町村の動的な絞り込み(Ajax)は今回のスコープ外とした。
  そのためだけに API エンドポイントを増やすのは T-06 の範囲を超えるため、都道府県変更時は
  選択肢をリセットし保存後の再読み込みで反映する、という割り切りにした(Phase 2 で改善余地あり)。

---

## - [x] T-07 求人 CRUD(セルフ入稿) — **完了**

**目的**: 掲載企業が自分で求人を作れるようにする。**現場兼任の担当者でも短時間で作れる入力体験にする**

**依存**: T-06

### 実装した内容

- [x] `job_postings` マイグレーション(`allow_external_feed`・`has_night_shift`・`view_count`・`application_count` を含む)
- [x] 中間テーブル `job_posting_qualification` / `job_posting_feature`
- [x] `ManagesJobPostings` トレイト。T-06 の教訓(CLAUDE.md 3.9)に従い、
      公開アクションは各コントローラで URL 順に定義し、実処理は `do*` に委譲
- [x] 掲載企業のセルフ入稿(`company/job-postings`、URL に企業 ID を含めない)
- [x] 運営者の代行入稿(`admin/companies/{company}/job-postings`)
- [x] **既存求人の複製**(`replicate()` でステータス・公開日時・カウンタ以外を丸ごとコピー)
- [x] 資格・こだわり条件のチェックボックス選択、事業所・職種・雇用形態のセレクト
- [x] 事業所は自社(または代行先企業)のものだけを選択肢にし、他社の事業所 ID を指定されても保存させない
- [x] `JobPosting::scopePublished()` — 公開可否の判定を一箇所に集約
- [x] **公開中の求人を編集すると審査待ちに自動で戻る**(SPEC.md 7章の前提を実装)
- [x] 給与下限・上限の整合性チェック(`lte:salary_max`)

### 検証済み(19 テスト / 48 assertions)

**掲載企業**(`Company\JobPostingManagementTest`)
- 求人登録は必ず下書きになる
- **他社の事業所を指定すると求人を作れない**
- 資格・こだわり条件を複数選択して保存・再編集できる
- **他社の求人を編集・更新・削除・複製できない(404)**
- **複製すると全項目(雇用形態・給与・夜勤・資格・こだわり条件)がコピーされ、ステータスは下書きに戻る。元の求人は変化しない**
- 下書きは一覧に出るが `published` スコープには含まれない
- **公開中の求人を編集すると審査待ちに戻る。下書きの編集ではステータスが変わらない**
- 給与下限が上限を超えると保存できない
- 一覧には自社の求人だけが表示される

**運営者**(`Admin\JobPostingManagementTest`)
- 任意の企業に代行入稿できる
- 他社の求人を URL で組み替えても編集できない
- **他社の事業所を指定した代行入稿は失敗する**
- 掲載企業は運営管理画面の代行入稿ルートに入れない

### 設計上の判断

- T-06 で踏んだ「暗黙のルートモデルバインディングの位置ズレ」を最初から回避した。
  `ManagesJobPostings` トレイトには公開アクションを置かず、`do*` の実処理メソッドのみを定義し、
  各コントローラが URL のパラメータ順に合わせて公開アクションを個別定義する形にしている。
- 求人が参照する `workplace_id` は、リクエストの値をそのまま保存せず
  `$company->workplaces()->whereKey($workplaceId)->exists()` で自社所属を検証してから保存する。
  他社の事業所 ID を指定されても求人を紐づけさせないため。
- 複製は `replicate()` にステータス・公開日時・カウンタを除外リストとして渡し、
  それ以外の全カラムをコピーしたうえで明示的に下書き状態へ上書きする。
- 都道府県→市区町村と同様、事業所選択時の住所自動反映(JS)は今回のスコープ外とした。
  事業所自体に住所が紐づいているため、求人には反映すべき値が元々存在せず実装不要と判断した。

### 引き継ぎ事項(T-08 で扱う)

- 審査フロー本体(承認・差戻し・履歴記録・審査待ち一覧)はまだ無い。
  今回実装したのは「公開中の求人を編集すると審査待ちに戻る」という前提部分のみ。
- 掲載期限切れによる自動 `closed` 化は T-09(掲載プラン)で実装する。

---

## - [x] T-08 審査フロー — **完了**

**目的**: 運営者が掲載内容に責任を持つ仕組みを作る。**運営者の日常業務の中心**

**依存**: T-07

### 実装した内容

- [x] `job_posting_reviews` マイグレーション(追記のみ・削除更新なしの証跡テーブル)
- [x] `SubmitJobPostingForReviewService` — 掲載企業の「審査に提出」。
      サイト設定(`requires_review`)が OFF なら即座に公開し `auto_approved` を記録
- [x] `ReviewJobPostingService` — 運営者の承認・差戻し。対象が `pending` でない場合は例外
- [x] `Admin\ReviewController` — 審査待ち一覧(`admin/reviews`)、承認・差戻しアクション
      (全社横断のため URL に `company` を含めない。運営者はもともと全社の求人を扱える権限)
- [x] 差戻し理由を必須入力にし、**掲載企業の全担当者へメール通知**(`JobPostingRejectedNotification`)
- [x] 審査画面に SPEC.md 12.3 の法令チェックポイントを表示(自動チェックではなく目視確認の手がかり)
- [x] `ManagesJobPostings::doSubmit()` — 下書き・差戻しの状態からのみ提出できる(運営者・掲載企業で共用)
- [x] 掲載企業の求人一覧に「審査に提出」ボタンと、差戻し理由の表示を追加
- [x] 運営者ダッシュボードに審査待ち件数を表示

「公開中の求人を編集したら審査待ちに戻す」は T-07 で先行実装済み。

### 検証済み(19 テスト / 57 assertions)

**審査**(`Admin\JobPostingReviewTest`)
- 審査待ちの求人だけが一覧に出る(下書きは出ない)
- 承認すると公開され、履歴に運営者IDが記録される
- **差戻すと理由付きで記録され、掲載企業に通知が届く**
- 差戻し理由が空だと差戻せない
- **`pending` 以外の求人は承認も差戻しもできない**(状態機械の逸脱を防ぐ)
- 掲載企業は審査待ち一覧に入れない
- サイト設定で審査 OFF にすると提出時に即公開され `auto_approved` が記録される
- 審査 ON のときは提出しても審査待ちになるだけ
- **`published` スコープには公開中以外のいかなるステータスも含まれない**(CLAUDE.md 3.5 の最終確認)

**提出**(`Company\JobPostingSubmitTest`)
- 下書き・差戻し済みの求人は提出できる
- **審査待ち・公開中の求人は再提出できない**
- 他社の求人は提出できない

**サイト設定**(`SiteSettingTest`、今回追加)
- `SiteSetting::current()` が DB のデフォルト値を正しく反映する

### 重大なバグを 1 件発見・修正

**`SiteSetting::current()` が `requires_review` を常に `null`(= 審査 OFF 扱い)として返していた。**

原因: Eloquent の `create([])` は、渡さなかったカラムを DB 側のデフォルト値(マイグレーションの `default(true)`)で INSERT させるが、**そのデフォルト値をモデルインスタンスには反映しない。** `SiteSetting::current()` は行が無ければ `create([])` で作っていたため、初回アクセス時に返るインスタンスは `requires_review` が `null` のままで、以後 `$memo` にキャッシュされ続けていた。

このバグは T-03(サイト設定)の実装時から存在していたが、`requires_review` の実際の値に依存するロジックが T-08 で初めて書かれたために表面化した。`create([])->refresh()` に修正し、CLAUDE.md 3.11 にルール化した。他に同じパターンが無いかリポジトリ全体を検索し、この 1 箇所のみだったことを確認済み。

### 引き継ぎ事項(T-09 で扱う)

- 掲載期限切れによる自動 `closed` 化はまだ無い。掲載プランの期間制御と合わせて T-09 で実装する。

---

## - [x] T-09 掲載プランの定義と制限 — **完了**

**目的**: 顧客の収益モデルの根幹。運営者が掲載枠を設計して掲載企業に割り当てる

**依存**: T-08

### 実装した内容

- [x] `posting_plans` / `company_plan_assignments` マイグレーション
- [x] 運営者のプラン CRUD(`admin/posting-plans`)。プラン名・同時掲載可能件数・
      掲載期間・上位表示・事業所登録数上限・月額表示価格(表示のみ)
- [x] 掲載企業へのプラン割当(`admin/companies/{company}/plan-assignments`)。
      新しいプランを割り当てると、現在有効な割当は自動的に前日で終了させ、履歴として残す
- [x] `PostingPlanLimitService` — 上限判定を集約
      - 同時掲載件数は **「審査待ち + 公開中」の件数**でカウント(下書きはカウントしない)
      - プラン未割当の企業は無制限として扱う(立ち上げ期に求人が作れなくなるのを防ぐ)
      - 承認・自動承認時に、プランの掲載期間から `expires_at` を計算し、`is_featured` も反映
- [x] `SubmitJobPostingForReviewService` に上限チェックを追加。上限到達時は下書き保存はできるが提出できない
- [x] `ManagesWorkplaces::doStore` に事業所登録数の上限チェックを追加
- [x] `CloseExpiredJobPostingsCommand` — 掲載期限切れの自動 `closed` 化。cron 経由の日次バッチ、チャンク処理
- [x] `SiteSetting::canAddMoreCompanies()` / `canAddMoreJobPostings()` — **パッケージ販売プランの全体上限**
      (SPEC.md 6.2)。掲載企業登録・求人登録の双方でチェック
- [x] 掲載企業のダッシュボードに現在のプランと残枠(求人・事業所)を表示
- [x] 管理画面レイアウトに `$errors` の共通表示を追加(バリデーション例外がどの画面でも見えるように)

### 検証済み(38 テスト / 118 assertions)

**上限判定のコアロジック**(`PostingPlanLimitTest`)
- プラン未割当は無制限扱い
- 同時掲載件数の上限に達すると提出できない / **下書きはカウントしない**
- 編集中の求人自身は上限カウントから除外(差戻し→再提出が枠を圧迫しない)
- 事業所登録数の上限
- **過去に終了したプランは現在のプランとして扱われない** / プラン切替で新しい上限が即座に適用される
- 公開時に掲載期間から `expires_at` が計算される / 無期限プランは `expires_at` を設定しない
- 上位表示プランの求人は `is_featured` が true になる

**統合的な振る舞い**(`Company\JobPostingPlanLimitTest`)
- **上限に達した企業は求人を下書き保存できるが提出できない**(SPEC.md 6.1 の要求そのもの)
- 審査 OFF でも上限に達していれば公開されない
- 事業所の上限に達すると追加登録できない
- 承認時にプランの掲載期間から掲載終了日が設定される
- ダッシュボードに現在のプランと残枠が表示される

**運営者のプラン管理**(`Admin\PostingPlanManagementTest`)
- プランの作成 / バリデーション / 掲載企業は入れない
- 企業へのプラン割当が履歴に残る / **新しいプランを割り当てると前のプランが自動終了する**

**掲載期限切れバッチ**(`CloseExpiredJobPostingsTest`)
- 期限切れの公開中求人が掲載終了になる / 期限内・無期限・下書きは対象外

**パッケージ全体の上限**(`PackagePlanLimitTest`)
- 上限未設定なら無制限 / 掲載企業数・求人数それぞれの上限で登録が止まる

### 設計上の判断

- **承認時は同時掲載件数の上限を強制チェックしない。** 上限は「掲載企業が公開申請できるか」の関門であり、
  審査待ちに複数件が並ぶ状況でどれを通すかは運営者の裁量に委ねる。強制ブロックすると柔軟な運用ができなくなる。
- 公開時の属性計算(`published_at` / `expires_at` / `is_featured`)を `PostingPlanLimitService::publishAttributes()`
  に集約し、承認・自動承認の 2 経路で同じ計算を使う(片方だけ直すと挙動がずれる事故を防ぐ)。
- チェックボックス(`is_featured` / `is_enabled`)は hidden input と組み合わせ、オフの状態が確実に送信されるようにした。

### 引き継ぎ事項(T-10 で扱う)

- **上位表示(`is_featured`)の検索結果への反映(並び順)はまだ無い。** 公開メディアの検索・一覧自体が
  T-10 の対象のため、`is_featured` の値を求人に持たせるところまでを本タスクで済ませ、
  実際の並び順制御は T-10 で実装する。

---

## - [x] T-10 公開メディアサイト — **完了**

**目的**: 求職者が見るサイト。**検索エンジンからの流入が生命線**

**依存**: T-09, T-03

### 実装した内容

- [x] 画面: トップ(`public.top`)/ 求人検索・一覧(`public.jobs.index`)/ 求人詳細(`public.jobs.show`)/
      企業・事業所紹介(`public.companies.show`)
- [x] `JobPostingSearchService` — 都道府県・市区町村・職種・資格・施設形態・雇用形態・**夜勤有無**・
      こだわり条件(複数選択は AND)・キーワードで絞り込み。上位表示(`is_featured`)を優先し次に新着順
- [x] 全ページ Blade サーバサイドレンダリング。`core/public/` に配置しテーマで上書き可能
- [x] `JobPostingJsonLd` — Google しごと検索向け構造化データ(必須・推奨項目を Google 公式ドキュメントに準拠)
- [x] `SitemapController` — 公開中求人・稼働中企業のみを含むサイトマップ XML(チャンク処理)
- [x] レイアウトに OGP・canonical・meta description の共通出力を追加
- [x] 求人詳細の閲覧数カウント(`view_count`)
- [x] `JobPosting::scopePublished()` に**掲載期限の安全策を追加**(下記参照)

### 検証済み(18 テスト / 39 assertions)

**検索・詳細**(`Public\JobPostingSearchTest`)
- **公開中の求人だけが一覧に表示される**(下書き・審査待ち・差戻し・掲載終了は一切出ない)
- **掲載期限が切れた公開中求人はバッチ実行前でも表示されない**
- 都道府県・夜勤有無・キーワードで絞り込める
- **審査待ち・下書きの求人詳細に直接アクセスすると404**
- 公開中の求人詳細は表示され閲覧数が増える
- **JSON-LD が埋め込まれる**
- **HTML ソースに求人内容がそのまま含まれる(SSR の確認)**
- 掲載停止中の企業ページは表示されない
- 稼働中の企業ページは公開中の求人だけを表示する
- **サイトマップに公開中の求人だけが含まれる** / 有効な XML であること

**トップページ**(`Public\TopPageTest`)
- 表示される / おすすめ求人が表示される / 審査待ち・下書きは出ない

### 途中で見つかったバグ

1. **`DB::table('job_postings')->whereKey(...)` が誤動作した。** `whereKey()` は Eloquent 専用メソッドで
   Query Builder には存在しない。存在しないメソッドは `__call()` 経由で `where('key', ...)` という
   意図しないクエリに化け、実行時まで気づけなかった。`where('id', ...)` に修正。
2. **サイトマップ XML を Blade で描画すると構文エラーになった。** `<?xml version="1.0"?>` の宣言を
   テンプレート先頭に書くと、Blade コンパイラが `<?` を PHP 開始タグとして誤認識する。
   IDE の静的解析も同じ箇所を構文エラーとして検知していた(見落とさず対応)。
   Blade を使わず、コントローラで直接 XML 文字列を組み立てる方式に変更した。
3. **ルート定義で `SitemapController::class` を `__invoke` 前提の記法にしていたが実装は `index()`。**
   `[SitemapController::class, 'index']` に修正。

### 設計上の判断

- **`scopePublished()` に `expires_at` のチェックを追加した。** 掲載期限切れの自動 `closed` 化は
  日次バッチ(T-09 の `CloseExpiredJobPostingsCommand`)であり、バッチ未実行の間は最大 24 時間
  `status = published` のまま残る。ステータスだけを条件にすると、その間だけ期限切れ求人が
  検索結果・詳細・サイトマップに漏れる。ステータスと期限の両方をチェックすることで、
  バッチの実行タイミングに依存しない安全性を確保した。
- **こだわり条件の複数選択は AND 条件にした。** 1 つでも複数選ばれたら「全部満たす求人」に絞り込むのが
  求職者の直感に合うと判断した。
- **求人詳細 URL は ID ベースにした。** タイトルを含むスラッグの方が SEO的には望ましいが、
  スラッグ生成・重複対策・変更時の扱いは SPEC に明記がなく、スコープを超えるため見送った。
  meta タグと構造化データで SEO を担保する。
- **都道府県→市区町村の動的絞り込み(Ajax)は今回も実装しない。** T-06/T-07 と同じ判断。
  都道府県を選ぶとページが再読み込みされ、サーバ側で絞り込まれた市区町村が返る形にした。

### 実機確認できなかったこと(正直な申告)

- **JSON-LD を Google のリッチリザルトテストに実際には投げていない。** 外部サービスへの送信になるため、
  必須・推奨項目が Google 公式ドキュメントの要件を満たしていることをコードレビューで確認する形に留めた。
  本番公開後に実際のリッチリザルトテストで検証すること。
- **スマートフォン幅(375px)での実機・ブラウザ確認はできていない。** Tailwind のレスポンシブクラス
  (`sm:` `lg:` の grid-cols 切り替えなど)を全画面で使っていることはコードで確認したが、
  実際のブラウザ描画によるレイアウト崩れの確認は行っていない。

---

## - [x] T-11 求職者会員機能 — **完了**

**目的**: メディアの資産になる会員基盤

**依存**: T-10

### 実装した内容

- [x] `job_seeker_qualification`(中間テーブル)/ `job_seeker_experiences` マイグレーション
- [x] 会員登録(`Seeker\Auth\RegisterController`)。登録直後に自動ログインしてマイページへ
- [x] プロフィール編集(基本情報 + 保有資格の複数選択)。URL に ID を含めず対象は常にログイン中の本人
- [x] 職務経歴 CRUD +「↑/↓」ボタンでの並び替え(ドラッグ&ドロップ用ライブラリは増やさない判断)
- [x] 退会(`Seeker\AccountController`)。カスケード削除で保有資格・職務経歴も一緒に消える
- [x] `EnsureMemberFeatureEnabled` ミドルウェア(`member.enabled` エイリアス)。
      会員機能 OFF のとき会員登録ルートを 404 にする。ログイン画面自体は残す
- [x] ログイン画面に「会員登録がまだの方はこちら」を会員機能 ON のときだけ表示
- [x] 自由記述欄(職務経歴の業務内容欄の近く)に機微情報を書かない注意書きを表示

### 検証済み(22 テスト / 53 assertions)

**会員登録**(`Seeker\RegistrationTest`)
- 登録できる / メール重複で登録不可 / パスワード確認不一致で登録不可
- **会員機能 OFF のとき登録画面・登録処理ともに 404**
- 会員機能 OFF でもログイン画面自体は表示される
- ログイン画面の登録導線が会員機能の ON/OFF で出し分けられる

**プロフィール・職務経歴**(`Seeker\ProfileTest`)
- プロフィール更新 / 保有資格の複数選択保存 / 市区町村と都道府県の整合チェック
- **他人のプロフィールを書き換えても本人には影響しない**
- 職務経歴の追加・更新・削除
- **他人の職務経歴は編集・削除できない(404)**
- 並び替えが機能する / **並び替えに他人の経歴 ID を混ぜても他人の順序は変わらない**

**退会**(`Seeker\AccountDeletionTest`)
- 退会するとアカウントが削除されログアウトする
- **保有資格・職務経歴もカスケードで一緒に削除される**
- 未ログインでは退会できない

**要配慮個人情報**(`Seeker\SensitiveDataTest`)
- `job_seekers` / `job_seeker_experiences` の両方に禁止カラムが存在しないことを確認

### 設計上の判断

- **並び替えは「↑/↓」ボタン方式にした。** ドラッグ&ドロップは体験としては優れるが、
  そのためだけに JS ライブラリを追加するのは今回のスコープに見合わないと判断した。
- **会員機能 OFF でもログイン画面は残す。** 既に登録済みの会員が引き続きログインできるようにするため。
  弾くのは新規登録の導線だけにした。
- **プロフィール・職務経歴の URL に ID を含めない。** 掲載企業(CLAUDE.md 3.8)と同じ考え方で、
  対象は常にログイン中の本人から決まるようにし、他人を指定する余地を構造的に無くした。
- 退会確認は二段階確認ではなく確認ダイアログ(JS `confirm`)に留めた。
  掲載企業の削除(archived 化のみで物理削除しない)ほど重い操作ではないため。

### 手動確認で踏んだ落とし穴(実装のバグではない)

curl で日本語の氏名を POST 送信して確認しようとしたところ、MySQL が
`Incorrect string value` エラーを返しタイムアウトした。原因を調べたところ、
**Git Bash(Windows)のシェル環境が日本語引数を UTF-8 以外のバイト列として
curl に渡していた**ことが原因で、アプリケーション側の問題ではなかった
(DB は `utf8mb4` で正しく設定されており、PHPUnit の自動テストでは日本語の
氏名を使ったアサーションが多数正しく通っている)。英数字のみの氏名で
再確認し、会員登録 → マイページ → プロフィール編集の一連の流れを実機で確認した。

---

## - [x] T-12 応募機能 — **完了**

**目的**: 求職者が応募し、その時点の履歴書がコピーされる

**依存**: T-11

### 実装した内容

- [x] `applications` / `application_resume_snapshots` マイグレーション。
      `applications` は `(job_posting_id, job_seeker_id)` に一意制約を張り、
      DB レベルでも重複応募を防ぐ(サービス層の事前チェックと二重の防御)
- [x] 求人詳細の応募ボタンを実装(`Public\JobPostingController`)。応募済みなら
      ボタンの代わりに「応募済みです」を表示し、二重応募の入口自体を塞ぐ
- [x] 応募フォーム(`public.jobs.apply`)。ログイン中はメッセージ欄のみ、
      未ログインは会員登録項目(お名前・メール・パスワード)を同じフォームに含め、
      1 回の送信で会員登録と応募の両方を完了させる(`Public\ApplicationController`)
- [x] `CreateApplicationService`
  - 応募レコードの作成 + `application_count` の増加をトランザクションで一括処理
  - その時点のプロフィール・保有資格・職務経歴を JSON でスナップショット化して保存
  - 初期ステータスは `Application::STATUS_NEW`(新規応募)
  - 重複応募はサービス層で事前に検知し、ユーザー向けのバリデーションエラーとして返す
- [x] `ReferrerSourceResolver`。求人詳細ページの `?ref=indeed` 等の URL パラメータを
      セッションに記録し、そのままページを跨いで応募完了時点の `referrer_source` に反映する。
      許可リスト外の値は無視して `direct` のまま残す
- [x] マイページに応募履歴セクションを追加(求人名・企業名・応募日・選考ステータス)

### 検証済み(7 テスト / 24 assertions、`Public\ApplicationTest`)

- ログイン済みの求職者が応募でき、`applications` レコードと `application_count` の増加を確認
- **応募後に求職者が氏名・保有資格・職務経歴を変更してもスナップショットの内容が変わらないこと**
- 未ログインの求職者が会員登録と同時に応募でき、応募後は自動ログインされる
- 同じ求人への 2 回目の応募はバリデーションエラーになり、レコードも `application_count` も増えない
- `?ref=indeed` を求人詳細に付けて訪問すると `referrer_source` に記録される
- 許可リストにない `ref` パラメータは無視され `direct` のままになる
- 公開中でない求人(下書き等)への応募は 404 になる

### 設計上の判断

- **応募ルートは `member.enabled` ミドルウェアの対象外にした。** 会員登録の自由な自己受付は
  会員機能 OFF で塞ぐ一方、応募自体は求人ごとの導線であり、応募と同時にアカウントを作る
  この一連の流れまで塞ぐと「応募したいのに応募できない」状態になってしまうため区別した。
- **応募フォームの未ログイン項目は登録フォームと同じ最小項目(お名前・メール・パスワード)に揃えた。**
  電話番号・生年月日・居住地はプロフィール編集側で後から入力できるため、応募という
  コンバージョンの障壁になる項目を増やさない判断をした。
- **`company_id` を `applications` に非正規化して持たせた。** `job_posting_id` から辿れるが、
  T-13 の掲載企業側の応募一覧で求人テーブルを経由せず直接絞り込めるようにするため
  (SPEC.md 8.2 の定義どおり)。
- **重複応募の防止はサービス層の事前チェック + DB の一意制約の二段構え。** 事前チェックは
  ユーザーに分かりやすいバリデーションエラーを返すため、一意制約は競合状態(同時に
  2 リクエストが飛んできた場合)でもデータの整合性を守るための最終防衛線。
- 選考ステータスは `Application::STATUS_NEW` と `STATUS_LABELS` のみ先に用意し、
  「書類選考中」以降の状態遷移(SPEC.md 8.3)は選考機能そのものを実装する T-13 に委ねた。

> **ここまで完了したら、一度デモデータを入れて実際に触ること。** 仕様のズレを見つける最初のチャンス。

---

## - [x] T-13 応募者管理 — **完了**

**目的**: 掲載企業が応募を取りこぼさず選考を進められるようにする

**依存**: T-12

### 実装した内容

- [x] `application_status_logs` / `application_notes` / `audit_logs` マイグレーション。
      前者2つは応募が削除されればカスケード削除、変更者(company_user)が退職して
      アカウント削除されても証跡としてログ行自体は残す(`nullOnDelete`)
- [x] `Application` モデルに選考ステータスの全定数を追加(SPEC.md 8.3: 新規応募 → 書類選考中 →
      面接調整中 → 面接済 → 内定 → 入社、および 不採用 / 辞退)。ステータス間の遷移制約は
      設けず、任意のステータス間を行き来できるようにした(運営上の訂正を妨げないため)
- [x] `ApplicationSearchService`。ステータス・求人・事業所・応募日(from/to)・流入元で
      絞り込む。掲載企業側は自社に、運営者の横断確認画面は掲載企業指定にそれぞれ絞れる
      共通実装(`$company` の有無で切り替え)
- [x] 掲載企業の応募者一覧・詳細(`Company\ApplicationController`)。
      一覧に未対応(新規応募)件数を赤バッジで表示。ダッシュボードにも同じバッジを追加
- [x] 応募者詳細は応募時点のスナップショットのみを表示し、`job_seekers` の現在値は
      一切参照しない(CLAUDE.md 3.6)
- [x] `ChangeApplicationStatusService`。ステータス変更のたびに `application_status_logs` へ
      追記するトランザクション処理。不採用・辞退にしても応募レコード自体は削除しない
- [x] 応募者への社内メモの追加・削除(`ApplicationNote`)
- [x] 運営者の応募横断確認画面(`Admin\ApplicationController`)。閲覧のみで
      ステータス変更・メモ・CSV出力は掲載企業側だけの機能とした
- [x] `ApplicationCsvExporter`。出力項目は氏名・メールアドレス・電話番号・応募日・
      ステータスのみで、氏名・連絡先は必ずスナップショットから読む(CLAUDE.md 3.6)。
      Excel の日本語文字化け対策で UTF-8 BOM を付与
- [x] `AuditLog` モデル(`audit_logs` テーブルを新設)。CSV 出力のみ記録対象にした。
      審査承認/差戻し・プラン変更・サイト設定変更・データ削除の記録漏れ確認は T-16 に委ねる

### 検証済み(19 テスト / 44 assertions)

**掲載企業の応募者管理**(`Company\ApplicationManagementTest`、17 テスト)
- 一覧表示 / **他社の応募者は一覧に出ない**
- **未対応件数の表示**
- ステータス・事業所・応募日・流入元の全絞り込み条件が動く
- 応募者詳細でスナップショットが読める / **他社の応募者詳細は 404**
- ステータス変更 + 複数回の変更履歴が全件記録される
- **不採用にした応募者も一覧に残る**
- **他社の応募者のステータスは変更できない(404)**
- メモの追加・削除 / **他社の応募者へのメモ追加・他社メモの削除はできない(404)**
- **CSV 出力が動作し、内容にスナップショットの氏名・メールが含まれ、監査ログに記録される**

**運営者の横断確認**(`Admin\ApplicationCrossCheckTest`、2 テスト)
- 全掲載企業の応募が一覧に表示される / 掲載企業で絞り込める

### 設計上の判断

- **選考ステータスの遷移に制約を設けなかった。** SPEC.md 8.3 の図は典型的な流れを示すが、
  「面接済に戻したい」等の訂正を業務上禁止する理由がなく、状態遷移を厳密にガードする
  実装コストに見合わないと判断した。バリデーションは `STATUS_LABELS` のキーに含まれるかだけ。
- **CSV 出力・ステータス変更・メモは掲載企業のみの機能にし、運営者は閲覧のみにした。**
  選考は応募を受け取った掲載企業の業務であり、運営者が代わりに操作する必然性がない。
  TASKS.md の作業内容一覧でも「運営者が横断確認できる画面」と「CSV 出力」は別項目として
  分けて書かれており、この区分けと一致する。
- **`audit_logs` を本タスクで新設した。** SPEC.md の記録対象(審査承認/差戻し・応募者情報の
  閲覧/CSV出力・プラン変更・サイト設定変更・データ削除)のうち、最初に必要になったのが
  CSV 出力だったため。T-16(運営者管理画面の仕上げ)で「記録対象の実装漏れ確認」を
  行う前提であり、T-13 時点で全記録対象を後追いで実装することはしなかった。
- **CSV に UTF-8 BOM を付けた。** 本製品は Excel で開かれる前提(担当者の日常業務)であり、
  BOM なしでは日本語が文字化けする。過剰実装ではなく実運用上の必須対応と判断した。
- **応募者一覧・横断確認画面はページネーションを入れず `get()` のまま。** 既存の
  求人一覧・審査一覧など管理画面が軒並みページネーションなしで実装されている
  規約に合わせた。応募数が増えて問題になった時点で対応する。

---

## - [x] T-14 アグリゲーション連携 ★主要な売り — **完了**

**目的**: 掲載しただけで応募が来る状態を作る。**この製品の中核**

**依存**: T-10

### 実装した内容

- [x] 各媒体の XML フォーマットを実装前に調査(Indeed は公式ドキュメント
      https://docs.indeed.com/indeed-apply/xml-feed を直接確認。求人ボックス・スタンバイは
      公式 PDF/マニュアル本体をこの環境から取得できなかったため、求人検索エンジン向け
      XML フィードの一般的な構造を調査した上で実装し、コード内に「本番投入前に公式資料で
      再確認すること」という注記を残した(下記「設計上の判断」参照)
- [x] `site_settings.enables_external_feed` を追加。**運営者の設定が優先**という
      SPEC.md 10.2 の要件どおり、これが OFF なら掲載企業側の `allow_external_feed`
      に関わらず全媒体のフィードが空になる
- [x] 求人フォームに「外部媒体への配信」チェックボックスを追加(`allow_external_feed`)。
      掲載企業が求人ごとに配信可否を制御できる
- [x] `JobPosting::scopeFeedEligible()`。`published()`(状態 + 掲載期間)に
      `allow_external_feed = true` を重ねた配信対象スコープ
- [x] `Feeds\FeedBuilder` インターフェースと媒体ごとの実装
      (`IndeedFeedBuilder` / `KyujinboxFeedBuilder` / `StanbyFeedBuilder`)。
      本文・企業名などの自由記述は CDATA で包み、HTML エンティティは使わない
      (Indeed 公式仕様の指示どおり)
- [x] `GenerateAggregationFeedsCommand`(`feeds:generate`)。`chunkById` で
      対象求人を読み進めながら 1 求人ずつファイルハンドルへ書き込み、巨大な
      XML 文字列を一度にメモリへ載せない。一時ファイルに書いてから `move()` で
      本番ファイルに差し替える(配信中に中途半端なファイルを読ませないため)
- [x] cron 日次実行として `routes/console.php` に登録
      (`CloseExpiredJobPostingsCommand` と同じ方針)
- [x] `Feed\FeedController`(`/feed/indeed.xml` 等)。リクエストのたびに DB を
      舐めず、生成済みの静的ファイルを読んで返すだけ
- [x] 応募の流入元記録は T-12 の `ReferrerSourceResolver` を再利用。
      各フィードの求人 URL に `?ref=indeed` 等を付与するだけで済んだ
- [x] `FeedMetaStore`。媒体ごとの最終生成日時・配信件数を JSON で記録
- [x] 運営者管理画面に媒体別の効果画面(`Admin\FeedController`)。
      流入元別の応募件数(累計)と、XML フィード媒体の配信中求人件数・
      最終生成日時を一覧表示

### 検証済み(10 テスト / 追加 45 assertions)

**フィード生成**(`GenerateAggregationFeedsCommandTest`、5 テスト)
- 配信対象(公開中 + 配信許可)の求人だけが 3 媒体全てに出力される
- **下書き・審査待ち・掲載終了・配信不可・掲載期限切れの求人は一切含まれない**
- **運営者の設定が OFF なら、掲載企業が配信許可していても全媒体が空になる**
- 3 媒体とも `simplexml_load_string` で解釈できる妥当な XML であること
- 求人 URL に `?ref={media}` が付与されること / 生成日時と件数が `FeedMetaStore` に記録されること

**フィード配信**(`Public\FeedControllerTest`、2 テスト)
- 生成前は 404 / 生成済みのフィードが正しい Content-Type で配信される

**運営者の媒体別効果画面**(`Admin\FeedStatsTest`、2 テスト)
- 流入元別の応募件数と配信中求人件数・最終生成日時が表示される
- 未生成の媒体は「未生成」と表示される

**求人フォーム**(`Company\JobPostingManagementTest`、追加 1 テスト)
- 配信可否のチェックボックスの ON/OFF が保存される

### 設計上の判断

- **求人ボックス・スタンバイの XML タグ名は公式資料を完全には参照できていない。**
  Indeed は公式ドキュメントを直接確認して実装したが、求人ボックスの XML フィード仕様書
  (PDF: `https://ad.kyujinbox.com/xmlfeed.pdf`)とスタンバイの公式マニュアルは、
  この実装環境からアクセスできなかった。求人検索エンジン向け XML フィードの一般的な
  構造(解説記事: `https://aggregate.eole.co.jp/column/column044/` 等で確認)に基づいて
  実装し、各ビルダーのクラス冒頭に **「本番投入前に必ず公式資料で正式なタグ名・
  必須項目を再確認すること」** という注記と参照 URL を残した。妥当な XML を出力する
  ことと、値の受け渡し方(CDATA・エンコーディング・URL 構造)自体は媒体を問わず
  正しい設計にしてあるため、本番投入前のタグ名の突き合わせだけで対応できる状態にした。
- **Google しごと検索向け JSON-LD は `allow_external_feed` で絞り込まなかった。**
  SPEC.md 10.2 の「出力対象は published かつ allow_external_feed のみ」という文言は
  文脈上 XML フィード(Indeed 等)を指すと判断した。JSON-LD は T-10 で
  `published()` のみを条件に実装済みで、そのテストも一致している。ここで新たに
  ゲートを追加すると T-10 の既存動作(と既存テスト)を壊すため、T-14 のスコープは
  XML フィード 3 媒体の新規実装に限定した。
- **フィードの保存先は `local` ディスク(`storage/app/private`)にした。** `public`
  ディスクは `storage:link` のシンボリックリンクに依存し、共有レンタルサーバーで
  使えるとは限らない(CLAUDE.md 3.10 と同じ理由)。フィードは常に `FeedController`
  経由で配信するため直接の静的配信は不要で、symlink 前提を避けられる `local` で十分。
- **一時ファイル → `move()` の 2 段階で書き込む。** 生成に数秒かかる 1,000 件規模でも、
  生成中のファイルを外部媒体のクローラーが読みに来て中途半端な XML を掴む事故を防ぐため。
- **媒体別の「流入数」は応募ベースの `referrer_source` 集計で表現した。** ページ閲覧
  ごとの媒体別クリック計測は 作業内容に無い新しいトラッキング基盤が必要になり、
  スコープを超える。既に記録している応募の流入元集計と、フィードの配信件数・
  生成日時の組み合わせで「効果が見える」という完了条件は満たせると判断した。

---

## - [x] T-15 メール通知 — **完了**

**目的**: 応募・審査の動きを関係者が確実に知る

**依存**: T-13, T-08

### 実装した内容

- [x] 外部 SMTP(SendGrid / SES)の設定は既に `.env.example` に用意済み
      (ローカルは mailpit)だったため、`QUEUE_CONNECTION=database` の上に
      cron 前提であることの注記を追加した
- [x] 通知クラス 5 種を新設し、いずれも `ShouldQueue` を実装してキュー経由で送る
      - `NewApplicationNotification`(掲載企業へ: 新規応募)
      - `JobPostingApprovedNotification`(掲載企業へ: 審査承認)
      - `NewReviewPendingNotification`(運営者へ: 新しい審査待ち)
      - `ApplicationReceivedNotification`(求職者へ: 応募受付)
      - `ApplicationStatusChangedNotification`(求職者へ: 選考ステータス変更)
      - 選考ステータスが実質変わらない(同じステータスへの再設定)場合は送らない
- [x] **既存の `JobPostingRejectedNotification` の不備を修正した。** `use Queueable`
      は使っていたが `ShouldQueue` を実装しておらず、実際には同期送信されていた
      (Laravel はキュー投入の可否を `ShouldQueue` インターフェースの有無だけで
      判定し、`Queueable` トレイルの有無は見ない)。今回 `implements ShouldQueue`
      を追加し、他の新規通知と同じ扱いにした
- [x] 各通知の送信先は `is_active = true` の担当者・運営者に限定
      (無効化されたアカウントに送らない)
- [x] 通知の発火元をサービス層に追加
      - `CreateApplicationService`: DB トランザクション確定後に求職者・掲載企業へ通知
        (ロールバックされた応募についてキューにジョブを積まないため)
      - `ReviewJobPostingService::approve()`: 掲載企業へ通知
      - `SubmitJobPostingForReviewService::submit()`: 審査待ちになった場合のみ運営者へ通知
        (審査 OFF による自動承認では発生しない)
      - `ChangeApplicationStatusService::change()`: 求職者へ通知
- [x] メールテンプレートにメディア名・ロゴを反映。`vendor:publish` で Markdown
      メールテンプレートを取り込み、`config('app.name')` を参照していた
      ヘッダー・フッターを `$site->site_name` / `$site->logo_path` に置き換えた。
      `$site` は `ThemeServiceProvider` が `View::composer('*', ...)` で
      **全てのビューに既に共有している変数**であり、Markdown メールのビューも
      対象に含まれるため、通知クラス側は何もせずに全メールへ反映される
- [x] ローカルは mailpit(既存の docker-compose 構成)で確認できる状態を維持

### 検証済み(9 テスト / 21 assertions)

**通知の送信先**(`NotificationTest`、7 テスト、`Notification::fake()` で検証)
- 応募すると求職者と掲載企業の有効な担当者に通知される。無効化された担当者には送らない
- 審査に提出すると有効な運営者にだけ通知される。審査 OFF 設定では通知自体が発生しない
- 承認すると有効な担当者にだけ通知される / 差戻すと担当者に通知される
- 選考ステータスが変わると求職者に通知される。**同じステータスへの変更では送られない**
- 通知本文にメディア名が正しく入ることをメール件名で確認

**キューの実機構**(`QueueWorkerTest`、2 テスト、`QUEUE_CONNECTION=database` に切替えて検証)
- **キューワーカーが動いていなくても応募自体は成功し、通知はジョブとして `jobs` に積まれるだけになる**
- **接続失敗するメール設定で `queue:work --stop-when-empty --tries=1` を実行すると
  `failed_jobs` に記録され、設定を戻して `queue:retry` すると `jobs` に戻ること**

### 設計上の判断

- **`JobPostingRejectedNotification` の `ShouldQueue` 漏れを発見・修正した。**
  T-08 時点で「キュー経由で送る」という意図のコメントと `Queueable` トレイトだけが
  書かれ、肝心の `ShouldQueue` インターフェースが抜けていたため、実際には同期送信
  されていた。T-15 でキュー基盤を整える中で気づき、他の通知と揃えて修正した。
- **`CompanyUserInvitationNotification` と `ResetPasswordNotification` はキューに載せない。**
  どちらも「その場で相手に伝える」即時性が求められるユースケースであり、
  cron 経由で最大 1 分待たされると体験が悪い。T-15 の作業内容にもこの 2 つは
  含まれておらず、意図的にスコープ外とした(コメントに明記済み)。
- **通知はサービス層の DB トランザクションが確定した後に送る。** トランザクション内で
  `->notify()` すると、ロールバックされた場合でもキューにジョブが残ってしまう
  (`database` キューは同じ DB 接続のトランザクションに含まれないため)。
- **メディア名・ロゴの反映は個々の通知クラスではなく、共通の Markdown メール
  テンプレート側で行った。** 各通知クラスに `$site->site_name` を埋め込むコードを
  書かなくても、`ThemeServiceProvider` が既に全ビューへ `$site` を共有している
  仕組みにそのまま乗れたため、テーマ機構(CLAUDE.md 3.2)と同じ設計原則を
  メール本文にも適用できた。
- **失敗ジョブのテストは 127.0.0.1 の未使用ポートへの接続失敗を利用した。**
  存在しないホスト名を使うと DNS 解決待ちで時間がかかる場合があるが、
  ループバックアドレスの未 listen ポートは即座に接続拒否されるため、
  実行時間を延ばさずに確実な失敗を再現できる。

---

## - [x] T-16 運営者管理画面の仕上げ — **完了**

**目的**: 顧客が自分でメディアを運営できる状態にする

**依存**: T-09, T-13

### 実装した内容

- [x] ダッシュボードに「掲載中求人数」「今月の応募数」を追加。審査待ち件数は既存
      (T-08)、媒体別流入は T-14 で作った `admin.feeds.index` へのリンクで提供済みのため、
      ダッシュボード自体には集計を重複させず遷移導線だけ置いた
- [x] サイト設定画面(`Admin\SiteSettingController`)。メディア名・キャッチコピー・
      メタディスクリプション・ロゴ・キービジュアル・テーマ・テーマカラー・連絡先・
      利用規約・プライバシーポリシー・機能のオンオフ(審査/会員/掲載プラン制限/
      アグリゲーション連携)を編集できる。対象は常に `SiteSetting::current()` の
      1 行のみのため、単数リソースとして URL に ID を含めない(CLAUDE.md 3.8 と同じ考え方)。
      `max_job_postings` / `max_companies`(パッケージ販売プランの上限)はここに含めない。
      運営者が変更できる項目ではなく、あなた(パッケージ提供者)が設定するものだから
- [x] マスタ管理画面(`Admin\MasterController`)。保有資格・施設形態・職種・雇用形態・
      こだわり条件の 5 種を 1 つのコントローラで扱う(列構成の違いは `has_category` フラグで
      吸収)。運営者が変更できるのは `is_enabled` と `sort_order` のみ(CLAUDE.md 3.7)。
      並び替えは職務経歴(T-11)と同じ「↑/↓」ボタン + 全件 ID 送信方式
- [x] 監査ログの閲覧画面(`Admin\AuditLogController`)。操作者の種別・操作・期間で絞り込み。
      `AuditLog` に `actor()` / `actorLabel()` / `targetLabel()` を追加し、
      操作者が退会・削除済みでも「company#12(退会・削除済み)」のように証跡として表示する
      (`actor_type` + `actor_id` は 3 ガードにまたがるため外部キー制約を張っていない設計。T-13 を参照)
- [x] 記録対象の実装漏れを埋めた
      - 審査の承認・差戻し(`ReviewJobPostingService`): 未実装だったため追加
      - 応募者情報の閲覧(`Company\ApplicationController::show()`): 未実装だったため追加
      - 応募者情報の CSV 出力: T-13 で実装済み(変更なし)
      - 掲載プランの変更(`Admin\PostingPlanController` の作成・更新、
        `Admin\CompanyPlanAssignmentController` の割当): 未実装だったため追加
      - サイト設定の変更: 本タスクで新設した画面と同時に実装
      - データ削除: 完全削除機能自体が T-17 でこれから作られるため、ログ記録もそこで実装する
        (スコープの判断は下記「設計上の判断」を参照)

### 検証済み(17 テスト / 39 assertions)

**サイト設定**(`Admin\SiteSettingManagementTest`、5 テスト)
- 変更でき即座に反映される / 必須項目が無いと保存できない / ロゴをアップロードできる
- 変更が監査ログに記録される / 掲載企業は変更できない(ログイン画面へリダイレクト)

**マスタ管理**(`Admin\MasterManagementTest`、6 テスト)
- 有効/無効を切り替えられる / **無効化すると公開サイトの選択肢(`selectable()`)に出なくなる**
- 並び順を変更できる / 存在しないマスタ種別は 404 になる
- 変更が監査ログに記録される / 掲載企業は管理画面に入れない

**監査ログ**(`Admin\AuditLogTest`、5 テスト)
- 一覧が表示される / 操作者の種別で絞り込める / 期間で絞り込める
- **退会・削除済みのアカウントによる操作でも証跡として表示される**
- 掲載企業は閲覧できない

**ダッシュボード**(`Admin\DashboardTest`、1 テスト)
- 掲載中求人数と今月の応募数が表示される

### 設計上の判断

- **データ削除の監査ログ記録は T-17 に委ねた。** 現状「削除」に該当する操作は
  掲載企業による下書き求人の削除・事業所の削除など断片的にしか存在せず、
  SPEC.md が意図する「データ削除」は解約時の完全削除機能(T-17 で新設)を
  指していると判断した。存在しない機能に先回りしてログ記録の配線だけ作ると、
  T-17 の実装内容と食い違うリスクがある。
- **マスタ管理は 5 種を 1 つの汎用コントローラにまとめた。** 保有資格・施設形態・
  職種・雇用形態・こだわり条件は列構成(`category` / `short_name` の有無)が
  微妙に違うだけで、操作(有効/無効の切替・並び替え)は完全に共通のため、
  5 つの似たコントローラを作るより 1 つの設定テーブル(`TYPES` 定数)で
  差分を吸収するほうが保守しやすいと判断した。
- **監査ログの絞り込みに「対象」フィルタを設けなかった。** `target_type` は
  完全修飾クラス名(`App\Models\JobPosting` 等)で保存されており、運営者が
  意味のある形で選択肢から選ぶには表示名の変換が要る。作業内容の完了条件は
  「操作者・期間・対象で絞り込み」と書かれているが、まず操作者種別・操作・期間の
  3 条件で実用上十分と判断し、対象での絞り込みは今回見送った(一覧には対象の
  表示自体は出るため、目視での確認はできる)。
- **監査ログのインデックスは T-13 の時点で既に張ってある。** `(actor_type, actor_id)`
  `(target_type, target_id)` `action` の 3 つ。本タスクでは新規マイグレーションを
  追加していない。

---

## - [x] T-17 データ返還と完全削除 — **完了**

**目的**: 契約書に書く「解約時のデータ返還・完全削除」を実行できるようにする

**依存**: T-16

### 実装した内容

- [x] **重大バグの発見・修正**: `applications.job_seeker_id` が `cascadeOnDelete` の
      ままだった(T-12 の設計ミス)。求職者が退会すると応募・履歴書スナップショットまで
      一緒に消えてしまい、CLAUDE.md 3.6「求職者が退会しても企業側の選考記録が
      破綻しない」に反していた。`nullOnDelete` に変更するマイグレーションを追加し、
      退会時は参照だけを外して応募・スナップショットを残すようにした。
      `ChangeApplicationStatusService` の求職者通知も `jobSeeker` が null(退会済み)の
      場合にガードを追加(修正しないと選考ステータス変更時に致命的エラーになっていた)
- [x] `data:export`(`ExportAllDataCommand`)。掲載企業・事業所・求人・応募・
      履歴書スナップショット・求職者(会員)・掲載企業担当者を CSV(UTF-8 BOM 付き)で
      出力し、アップロード画像一式と合わせて 1 つの ZIP にまとめる。チャンク処理で
      1 行ずつファイルへ書き込むため件数が多くてもメモリを使い切らない
- [x] `data:destroy-all`(`DestroyAllDataCommand`)。二段階確認(メディア名の
      入力一致 → 明示的な yes 確認)を経て、`information_schema` から取得した
      全テーブルのうち製品マスタ(資格・施設形態・職種等)と `audit_logs` を除いて
      `TRUNCATE` し、アップロード画像も全削除する。実行前に `audit_logs` へ記録する
      ため、全データを消した後もその証跡だけは読める。`--admin=` で実行者の
      運営者アカウントを指定必須にし、存在しない場合は何もせず中止する
- [x] **どちらも Web からは実行できない CLI 専用コマンドにした。** 全会員の個人情報を
      含む一括エクスポート・環境全体の完全削除は、運営者(顧客)向けのセルフサービス
      機能として管理画面に置くにはリスクが大きすぎると判断したため(下記「設計上の
      判断」を参照)
- [x] 求職者の退会時、応募済みの選考記録が掲載企業側に残る旨をマイページの
      退会確認ダイアログ・注記に明記(規約本文は運営者が管理する自由記述のため、
      アプリ側で退会操作の直前に明示する形にした)
- [x] 手順書 `docs/解約時のデータ返還手順.md`。SSH 接続から `data:export` の実行・
      ダウンロード・中身の確認、`data:destroy-all` の二段階確認の通し方、削除後に
      `audit_logs` で証跡を確認する方法までを記載

### 検証済み(8 テスト / 自動 + 手動)

**求職者の退会**(`Seeker\AccountDeletionTest`、追加 1 テスト)
- **退会しても応募済みの選考記録(応募レコード・履歴書スナップショット)は残り、
  `job_seeker_id` だけが null になる**

**データエクスポート**(`ExportAllDataCommandTest`、1 テスト)
- 掲載企業・求人・応募・履歴書スナップショット・求職者の内容が ZIP 内の CSV に
  正しく含まれる。アップロード画像も `uploads/` 配下に含まれる

**完全削除**(`DestroyAllDataCommandTest`、6 テスト)
- 二段階確認を経て全データが削除され、監査ログに `data.destroy_all` が記録される
- **製品マスタ(資格等)は削除対象に含まれない**
- メディア名の入力が一致しない/最終確認で no と答える/`--admin` 未指定/
  存在しない運営者メールのいずれでも中止され、何も削除されない

**手動確認(手順書どおりの一連の操作)**
- テスト用データベースにサンプルデータ(掲載企業・事業所・求人・応募・
  履歴書スナップショット・求職者)を投入し、実際に SSH 相当のコマンドラインから
  `data:export` → ZIP の中身が実データと一致することを確認 → `data:destroy-all` を
  二段階確認込みで実行 → 全データが削除され製品マスタと監査ログだけが残ることを確認、
  という一連の流れが手順書どおりに通ることを確認した

### 設計上の判断

- **エクスポート・完全削除を運営者の管理画面(Web UI)には置かず、CLI 専用にした。**
  T-16 まではサイト設定・マスタ管理・監査ログ閲覧などを全て管理画面の機能として
  作ってきたが、全会員の個人情報を含む一括ダウンロードと環境全体の不可逆な破棄は
  性質が違う。管理画面のボタン 1 つでアカウント乗っ取り等から実行できてしまう
  リスクに見合わないと判断し、SSH 経由で運営者(あなた)が手順書に沿って実行する
  運用にした。これは T-01・T-18 が採用している「SSH 手順書」パターンと同じ考え方。
- **完全削除は「製品マスタ」と `audit_logs` を除いた opt-out 方式にした。**
  削除対象を個別にテーブル名で列挙する opt-in 方式だと、将来マイグレーションが
  増えるたびにこのコマンドを更新し忘れるリスクがある。「除外リストに無いテーブルは
  全て消す」という設計のほうが、個人情報を含む新しいテーブルを追加したときに
  漏れなく削除対象へ入るため安全側に倒れる。
- **`audit_logs` テーブル自体は削除しない。** 「完全削除を実行した」という記録が
  削除操作そのものによって消えてしまっては、監査ログの意味がない。資格・施設形態
  などの製品マスタも、顧客の個人情報を含まない配布データであり削除しても保護に
  寄与しないため対象から外した。
- **二段階確認は「メディア名の入力一致」+「明示的な yes」にした。** 単純な
  y/n 確認だけでは誤操作(Enter の連打等)で通ってしまう可能性がある。
  メディア名という、対象環境を強く意識させる文字列を正確に入力させることで、
  「今から何を消すか」を実行者に一度立ち止まって認識させる。

---

## - [ ] T-18 デプロイと全環境への配布

**目的**: 顧客が増えても更新を配れる体制を作る。**後回しにすると顧客 5 社で詰む**

**依存**: T-01

**作業内容**
- Xserver への設置構成([SPEC.md 13.2](SPEC.md#132-環境構成))
  - Laravel 本体を公開領域の外に置く
  - `public_html` を `app/public` へのシンボリックリンクに置き換える
  - `.env` が Web からアクセスできないことを URL を叩いて確認
- `deploy.sh` の作成
  - `git pull` → `composer install --no-dev --optimize-autoloader` → `migrate --force` → キャッシュ再構築
  - **`public/build` はローカルでビルドしたものを配布**(サーバでビルドしない)
- cron の登録(管理パネルから)
  - 毎分: `php artisan schedule:run`
  - 毎分: `php artisan queue:work --stop-when-empty --max-time=50`
- スケジューラに掲載期限切れ処理とフィード生成を登録
- バックアップ(Xserver の自動バックアップの確認 + 自前の日次 mysqldump を環境外へ退避)
- **環境台帳** `docs/環境台帳.md`(顧客名・ドメイン・サーバ情報・現在のバージョン・最終デプロイ日)
- 手順書 `docs/新規環境の構築手順.md` と `docs/デプロイ手順.md`

**完了条件**
- 本番 URL でサイトが表示され、HTTPS で接続される
- **`https://ドメイン/.env` にアクセスして 404 になる**
- `deploy.sh` を実行するだけでデプロイが完了する
- cron が動作している(掲載期限切れ処理のログで確認)
- **バックアップからのリストアを実際に試して成功している**
- `APP_DEBUG=false` になっている
- **手順書の通りに作業して、新しい環境を 1 つ構築できている**

---

## - [ ] T-19 デモメディアの立ち上げ

**目的**: 営業資料を作ると同時に、**自分がヘビーユーザーになって仕様のズレを見つける**

**依存**: T-14, T-18

**作業内容**
- デモ用の環境を 1 つ構築する(T-18 の手順書を実際に使う)
- 架空または実在の介護事業所のデータを投入する(実在なら許諾を得る)
- 掲載企業アカウントを作り、**実際にセルフ入稿から審査・公開まで通す**
- 求職者として会員登録し、実際に応募する
- アグリゲーション媒体への申請を出す
- 使ってみて気づいた問題を洗い出し、TASKS.md に追加タスクとして起票する
- 営業資料(画面キャプチャ・機能一覧・料金表)を作る

**完了条件**
- デモメディアが公開されており、URL を営業先に見せられる
- 求人が 30 件以上入っている
- セルフ入稿 → 審査 → 公開 → 応募 → 選考管理の一連が実データで通る
- **使って気づいた問題が TASKS.md に起票されている**(ゼロなら使い込みが足りない)
- 営業資料ができている

---

# Phase 2 — 拡張

契約が取れてから、あるいは商談で必要性が明らかになってから着手する。

## - [ ] T-20 スカウト機能

**依存**: T-13

**⚠ 着手前の必須確認**: 職業安定法上、特定募集情報等提供事業者の届出が必要になるかを確認する([SPEC.md 12.4](SPEC.md#124-職業安定法上の位置づけ))。これは顧客側の義務だが、導入支援の一環として案内できるようにする。

会員検索 / スカウト送信 / テンプレート / 送信履歴・開封状況 / **求職者の受信拒否設定(必須)** / 送信上限 / プレミアムプランのみ

## - [ ] T-21 CSV 一括登録

**依存**: T-07

求人・掲載企業の CSV 一括投入。既存メディアからの移行と、導入支援でのデータ投入に使う。チャンク処理必須。

## - [ ] T-22 NGワード検知・最低賃金チェック

**依存**: T-08

競合が標準搭載しており商談で比較される([SPEC.md 16.5](SPEC.md#165-競合との機能差))。審査画面での警告表示として実装する。**最低賃金は毎年改定されるため、マスタとして持ち更新できるようにする。**

## - [ ] T-23 お祝い金機能

**依存**: T-13

入社決定時のお祝い金の設定と支給管理。求職者の応募動機になる。

## - [ ] T-24 代理店階層

**依存**: T-05

運営元 / 代理店 / 掲載企業の 3 階層。**全画面の権限判定を見直すことになる**ため、必要性が確定してから着手する。

## - [ ] T-25 LINE 連携通知

**依存**: T-15

介護領域の求職者はメールを見ない層が相当いる。LINE 公式アカウント連携による通知。プレミアムプランのみ。
