# TASKS.md — 実行計画

仕様は [SPEC.md](SPEC.md)、実装ルールは [CLAUDE.md](CLAUDE.md) を参照。

## この文書の使い方

- **上から順に進める。** 依存が明記されているタスクは、依存先が完了してから着手する
- 1 タスク = 1 セッションで完結する粒度に切ってある
- **完了条件(DoD)を全て満たしてから次へ進む。** 一つでも欠けていればそのタスクは未完了
- 完了したら `- [ ]` を `- [x]` に変え、コミットする

## 進捗

| フェーズ | タスク数 | 状態 |
|---|---|---|
| Phase 0 基盤 | 4 | 未着手 |
| Phase 1 第1リリース | 15 | 未着手 |
| Phase 2 拡張 | 6 | 未着手 |

## 第1リリースの全体像

第1リリースは重い。**T-18 のデモメディア立ち上げを最後まで残さず、T-12(応募)が通った時点で一度データを入れて動かす**こと。自分がヘビーユーザーになるのが、仕様のズレを見つける唯一の方法(→ [SPEC.md 16.1](SPEC.md#161-第1リリースが重い))。

---

# Phase 0 — 基盤

## - [~] T-01 開発環境の構築と本番環境の実測 — **ローカル完了 / 本番調査が残**

**目的**: ローカル環境を作り、**本番(エックスサーバー)の実効値を調べて設計の前提を確定する**

### 完了した内容

- [x] git リポジトリを初期化(親の `c:\Users\hismi` が別リポジトリだったため独立させた)
- [x] Laravel **13.8** を導入
- [x] PHP を **8.4** に決定(→ [SPEC.md 13.3](SPEC.md#133-php-バージョンの選定))
- [x] `composer.json` の `config.platform.php` を `8.4.0` に固定(依存解決を本番に合わせる)
- [x] `docker-compose.yml`(app / mysql / mailpit / node)と `docker/php/Dockerfile`
      - 本番と同じ **Apache + PHP 8.4** 構成にし、`public` をドキュメントルートにした
      - 拡張は Xserver にあるものだけ(pdo_mysql / mbstring / gd / zip / bcmath / exif)
- [x] Tailwind CSS 4 + Vite 8(Laravel 13 に標準同梱)
- [x] `.gitignore` から `public/build` を除外(本番でビルドできないため成果物を配布する)
- [x] `.env.example` を整備。**Redis の設定項目を削除**し、session / queue / cache を `database` に固定
- [x] `config/app.php` の `timezone` を `Asia/Tokyo` に変更
- [x] `phpunit.xml` のテスト DB を sqlite → **MySQL(`kyujin_testing`)** に変更(→ [SPEC.md 13.4](SPEC.md#134-テストは本番と同じ-mysql-で走らせる))
- [x] `README.md` に起動手順と本番との差異を記載
- [x] `docs/Xserver実機調査.md` に調査手順を用意

**検証済み**
- `docker compose up -d --build` → http://localhost:8080 が 200 で表示される
- `php artisan migrate` が MySQL に対して成功
- `php artisan test` が MySQL に対して成功(2 passed)
- `npm run build` で `public/build` が生成され、git の追跡対象になっている
- `vendor` / `node_modules` / `.env` は除外されている

### 残っている作業(要: エックスサーバーの契約と SSH アクセス)

- [ ] `docs/Xserver実機調査.md` の手順を実行する
- [ ] **サーバーパネルで PHP を 8.4 に切り替える**(新規ドメインの初期値は 8.3)
- [ ] 調査結果を SPEC.md 13.1 の「要確認事項」に反映し、確定した値に書き換える
- [ ] 実測値に合わせて `docker/php/php.ini` を調整する

> **この調査結果は T-18(デプロイ)の方針を左右する。**
> git が使えなければ rsync ベースに、`symlink()` が使えなければ `public_html` の構成を変更することになる。
> **ただし T-02 以降の実装は調査を待たずに進められる。**

---

## - [x] T-02 介護特化マスタとシーダー — **完了**

**目的**: 業界特化の実体を用意する。**汎用パッケージとの差はここに集約される**

**依存**: T-01

### 実装した内容

- [x] マイグレーション 7 テーブル: `prefectures` / `cities` / `qualifications` / `facility_types` / `job_categories` / `employment_types` / `job_features`
- [x] 全マスタに `code`(製品が定める安定した識別子)・`is_enabled`・`sort_order`
- [x] `qualifications` / `facility_types` / `job_features` にカテゴリ列(画面でグループ表示するため)
- [x] `facility_types` に `short_name`(特養・老健・サ高住など現場の呼び方。検索のヒット率が上がる)
- [x] `app/Models/Concerns/IsMaster.php` — `enabled()` / `ordered()` / **`selectable()`** スコープ
- [x] `database/seeders/MasterSeeder.php` — 冪等な `sync()`。**顧客が管理する項目を上書きしない**
- [x] 各マスタのシーダーと `DatabaseSeeder`
- [x] `masters:import-cities` コマンド(総務省の全国地方公共団体コード CSV から全件取り込み)

### 投入件数(SPEC.md 11章と一致)

| マスタ | 件数 |
|---|---|
| 都道府県 | 47 |
| 市区町村 | 74(主要のみ。→ 下記の残作業) |
| 保有資格 | 22 |
| 施設形態 | 27 |
| 職種 | 12 |
| 雇用形態 | 6 |
| こだわり条件 | 20 |

### 検証済み(`tests/Feature/MasterDataTest.php` 9件 / 33 assertions)

- 全マスタが仕様どおりの件数で投入される
- 介護領域の主要マスタが漏れなく入っている / 略称が正しく返る
- 都道府県が 8 地方すべてをカバーしている
- **無効にしたマスタが選択肢に出てこない**
- 選択肢が顧客の指定した並び順で返る
- **シーダーを再実行しても行が重複しない**
- **シーダーを再実行しても顧客が調整した `is_enabled` / `sort_order` が消えない** ← 最重要
- シーダーを再実行すると製品が管理する項目(name / category)は最新に戻る
- 市区町村が都道府県に正しく紐づく

### 仕様変更

- **`workplaces.city`(文字列)→ `city_id`(マスタ参照)** に変更した。
  文字列では表記ゆれで市区町村の絞り込み検索が成立しないため。SPEC.md 8.2 を更新済み。
- SPEC.md に **11.0 マスタの所有権**(製品が管理する項目 / 顧客が管理する項目)と **11.7 市区町村マスタの網羅性** を追加。
- CLAUDE.md に絶対ルール **3.7 マスタの `is_enabled` / `sort_order` を製品側で書き換えない** を追加(以降の番号を繰り下げ)。

### 残作業(本番導入前に必須)

- [ ] **市区町村を全件取り込む。** 現在は主要 74 件のみで、全国約 1,900 の市区町村を網羅していない。
      介護事業所は全国の町村にも存在するため、このままでは登録できない事業所が出る。
      `php artisan masters:import-cities <総務省の全国地方公共団体コード CSV>`

---

## - [ ] T-03 サイト設定とテーマ機構 ★製品の生命線

**目的**: 「コードは1本」と「デザインは個別制作」を両立させる仕組みを作る

**依存**: T-01

**作業内容**
- `site_settings` テーブル(1レコード)とマイグレーション([SPEC.md 8.2](SPEC.md#82-主要テーブル))
- 設定値を取得するヘルパー(キャッシュ付き)
- **テーマ解決機構**
  - ビューの探索順を `resources/views/themes/{site_settings.theme}/` → `resources/views/core/` にする
  - `View::prependLocation()` またはビューファインダのパス追加で実装する
  - テーマの CSS・画像は `public/themes/{theme}/` から配信する
  - テーマ名を出力するヘルパー(`theme_asset()` など)
- `core/` に最小のレイアウト(ヘッダー・フッター・共通コンポーネント)を置く
- `themes/default/` を作り、標準テーマとする
- 運営者管理画面でテーマを切り替えられるようにする(まだ画面がないので設定値の変更だけでよい)

**完了条件**
- `themes/{theme}/` に同名の Blade を置くと、そちらが使われる
- テーマに無いファイルは `core/` にフォールバックする
- テーマを切り替えても全ページが 200 で表示される
- テーマの CSS・画像が正しく配信される
- **上記 3 点のテストが通る**
- CLAUDE.md 3.2 の「テーマに書いてよいもの/だめなもの」がこの実装で成立していることを確認済み

---

## - [ ] T-04 3種類の認証

**目的**: 運営者 / 掲載企業 / 求職者のログインを分離する

**依存**: T-01

**作業内容**
- `admin_users` / `company_users` / `job_seekers` のマイグレーション
- `config/auth.php` に 3 ガード(`admin` / `company` / `seeker`)を定義
- 各ログイン画面・ログアウト・パスワードリセット
  - 運営者: `/admin/login`
  - 掲載企業: `/company/login`
  - 求職者: 公開サイト内の `/login`
- **`tenant_id` に相当するカラムを作らない**(CLAUDE.md 3.8)
- ログイン画面は `core/` に置き、テーマで上書きできるようにする

**完了条件**
- 3 種類のログインがそれぞれ独立して動作する
- 掲載企業としてログインした状態で `/admin` にアクセスすると弾かれる
- 求職者としてログインした状態で `/company` にアクセスすると弾かれる
- 各ガードのログイン・ログアウト・認可のテストが通る

---

# Phase 1 — 第1リリース

## - [ ] T-05 掲載企業の管理

**目的**: 運営者が掲載企業を登録し、担当者アカウントを発行できるようにする

**依存**: T-04

**作業内容**
- `companies` マイグレーション([SPEC.md 8.2](SPEC.md#82-主要テーブル))
- 運営者管理画面の掲載企業 CRUD
- 担当者アカウントの発行と招待メール送信
- 掲載企業側の「企業情報編集」画面(自社のみ)
- 企業ロゴのアップロード(WebP 変換)

**完了条件**
- 運営者が掲載企業を登録し、担当者がログインできる
- 掲載企業が自社の情報を編集できる
- **他社の企業情報にアクセスすると 404 になるテストが通る**
- FormRequest でバリデーションしている

---

## - [ ] T-06 事業所・勤務地の管理

**目的**: 求人の前提となる事業所を登録できるようにする

**依存**: T-05

**作業内容**
- `workplaces` マイグレーション
  - 住所は `prefecture_id` + **`city_id`**(マスタ参照)+ `address`(番地以降のみ自由入力)
  - 市区町村を文字列にすると表記ゆれで絞り込み検索が成立しないため(SPEC.md 8.2)
- 掲載企業の管理画面で事業所 CRUD
- 施設形態・都道府県・市区町村はマスタから選択。**必ず `selectable()` を通す**(CLAUDE.md 3.7)
- 都道府県を選ぶと市区町村の選択肢が絞り込まれる
- 事業所写真のアップロード(WebP 変換)
- 運営者も代行して編集できるようにする

**完了条件**
- 事業所の登録・一覧・編集・削除ができる
- 無効化したマスタが選択肢に出てこない
- 都道府県に紐づく市区町村だけが選択肢に出る
- 写真がアップロードでき、WebP に変換される
- **他社の事業所に 404 が返るテストが通る**

> **前提**: 市区町村マスタは主要 74 件しか入っていない。T-02 の残作業(全件取り込み)を先に済ませないと、
> 主要都市以外の事業所を登録できずテストが現実的でなくなる。

---

## - [ ] T-07 求人 CRUD(セルフ入稿)

**目的**: 掲載企業が自分で求人を作れるようにする。**現場兼任の担当者でも短時間で作れる入力体験にする**

**依存**: T-06

**作業内容**
- `job_postings` マイグレーション([SPEC.md 8.2](SPEC.md#82-主要テーブル)。`allow_external_feed` と `has_night_shift` を忘れない)
- 中間テーブル `job_posting_qualifications` / `job_posting_features`
- 掲載企業の管理画面で求人 CRUD
- **入力負担を下げる仕組み**([SPEC.md 11.6](SPEC.md#116-入力負担を下げる仕組み))
  - 事業所を選ぶと住所・アクセス・施設形態が自動で入る
  - 資格・こだわり条件はチェックボックス
  - 職種・雇用形態はセレクト
  - **既存求人の複製**(「この求人を複製」ボタン)
- 下書き保存
- 運営者による代行入稿(立ち上げ期に必須)
- **フォーム項目は固定。項目を追加する仕組みを作らない**(CLAUDE.md 3.3)

**完了条件**
- 求人の登録・一覧・編集・複製・削除ができる
- 事業所を選択すると住所等が自動反映される
- 資格・こだわり条件を複数選択して保存・再編集できる
- 複製すると全項目がコピーされた下書きが作られる
- 下書きは公開サイトに表示されない
- 運営者が任意の掲載企業の求人を代行入稿できる
- **他社の求人に 404 が返るテストが通る**

---

## - [ ] T-08 審査フロー

**目的**: 運営者が掲載内容に責任を持つ仕組みを作る。**運営者の日常業務の中心**

**依存**: T-07

**作業内容**
- `job_posting_reviews` マイグレーション
- 状態遷移([SPEC.md 7章](SPEC.md#7-審査フロー))
  - 下書き → 審査待ち → 公開中 / 差戻し
  - **公開中の求人を編集したら審査待ちに戻す**
  - 掲載期限切れで掲載終了
- 運営者管理画面の「審査待ち一覧」と審査画面
- 差戻し時は理由を必須入力とし、掲載企業へメール通知
- 審査の判断を全て履歴に記録
- 審査画面に法令チェックポイントのチェックリストを表示([SPEC.md 12.3](SPEC.md#123-労働関連法令))
- サイト設定で審査を OFF にでき、その場合は提出即公開になる

**完了条件**
- 掲載企業が求人を提出でき、運営者の審査待ち一覧に出る
- 承認すると公開され、差戻すと理由つきで掲載企業に通知が届く
- **公開中の求人を編集すると審査待ちに戻る**
- 審査履歴が全件記録される
- 審査 OFF の設定で提出即公開になる
- **審査を通っていない求人が公開サイトに出ないテストが通る**

---

## - [ ] T-09 掲載プランの定義と制限

**目的**: 顧客の収益モデルの根幹。運営者が掲載枠を設計して掲載企業に割り当てる

**依存**: T-08

**作業内容**
- `posting_plans` / `company_plan_assignments` マイグレーション
- 運営者管理画面でのプラン定義([SPEC.md 6.1](SPEC.md#61-掲載プラン顧客が掲載企業に売るもの))
  - プラン名 / 同時掲載可能件数 / 掲載期間 / 上位表示 / 事業所登録数上限 / 月額表示価格
- 掲載企業へのプラン割当(開始日・終了日)
- **制限の実装**
  - 同時掲載件数の上限に達したら公開申請できない(保存はできる)
  - 掲載期間を過ぎたら自動で掲載終了(日次バッチ)
  - 事業所登録数の上限
  - 上位表示の反映(検索結果の並び順)
- 掲載企業のダッシュボードに残枠を表示
- **パッケージ販売プランの上限**(登録可能求人数・掲載企業数)も `site_settings` で制御する

**完了条件**
- 運営者がプランを定義し、掲載企業に割り当てられる
- 上限に達した掲載企業が求人を公開申請できず、理由が画面に出る
- 掲載期間を過ぎた求人が自動で掲載終了になる
- 上位表示のプランの求人が検索結果で優先される
- 掲載企業のダッシュボードに残枠が正しく表示される
- **プラン制限のテストが全項目で通る**

---

## - [ ] T-10 公開メディアサイト

**目的**: 求職者が見るサイト。**検索エンジンからの流入が生命線**

**依存**: T-09, T-03

**作業内容**
- 画面: トップ / 求人検索・一覧 / 求人詳細 / 事業所・企業紹介
- 求人検索の絞り込み: 都道府県・市区町村・職種・資格・施設形態・雇用形態・**夜勤有無**・こだわり条件
- **Blade によるサーバサイドレンダリング**
- テーマ機構の適用(全ページを `core/public/` に置き、テーマで上書き可能にする)
- SEO
  - 求人詳細に **JobPosting の JSON-LD** を出力
  - サイトマップ XML の自動生成
  - OGP・メタタグ
  - 画像の WebP 変換と遅延読み込み
- スマートフォン最適化(介護領域の求職者はスマホが主)
- 公開対象は `status = published` かつ掲載期間内の求人のみ

**完了条件**
- 検索の絞り込みが全条件で動作する
- 下書き・審査待ち・差戻し・掲載終了の求人が一切表示されない
- ページの HTML ソースに求人内容がそのまま含まれている(SSR の確認)
- JSON-LD が Google のリッチリザルトテストで妥当と判定される
- サイトマップ XML に公開中の求人だけが含まれる
- スマートフォン幅(375px)でレイアウトが破綻しない
- テーマを切り替えると見た目が変わる

---

## - [ ] T-11 求職者会員機能

**目的**: メディアの資産になる会員基盤

**依存**: T-10

**作業内容**
- `job_seeker_qualifications` / `job_seeker_experiences` マイグレーション
- 求職者マイページ: プロフィール / 保有資格 / 職務経歴 / 応募履歴 / 退会
- 職務経歴は複数件を追加・並び替え・削除できる
- **健康状態・病歴・障害に関する項目を作らない**(CLAUDE.md 3.4)
- 自由記述欄に機微情報を書かないよう注意書きを表示
- サイト設定で会員機能を OFF にできる(OFF のときは都度応募のみ)

**完了条件**
- 求職者が登録・ログイン・プロフィール編集・退会できる
- 保有資格をマスタから複数選択できる
- 職務経歴を複数件追加・並び替え・削除できる
- **テーブル定義に健康状態・病歴・障害・国籍・信条のカラムが存在しない**
- 自由記述欄の近くに注意書きが表示されている
- 会員機能 OFF の設定で会員登録画面が出なくなる

---

## - [ ] T-12 応募機能

**目的**: 求職者が応募し、その時点の履歴書がコピーされる

**依存**: T-11

**作業内容**
- `applications` / `application_resume_snapshots` マイグレーション
- 求人詳細の応募ボタン → 応募フォーム
- ログイン済ならプロフィールから自動入力、未ログインなら会員登録と同時に応募できる導線
- `CreateApplicationService`
  - 応募レコードの作成
  - **その時点の履歴書内容を JSON でスナップショット化して保存**
  - 初期ステータスを「新規応募」に設定
  - **流入元(`referrer_source`)を記録**(URL パラメータから判別)
  - トランザクションで囲む
- 同一求人への重複応募を防ぐ
- 求人の `application_count` を更新
- **応募フォームの項目は固定。追加する仕組みを作らない**(CLAUDE.md 3.3)

**完了条件**
- 求職者が応募でき、掲載企業に応募レコードが作られる
- **応募後に求職者がプロフィールを変更しても、スナップショットの内容が変わらない**
- 未ログインの求職者が「会員登録して応募」で一気に完了できる
- 同じ求人に 2 回応募できない
- 流入元が記録される
- スナップショット生成のテストが通る

> **ここまで完了したら、一度デモデータを入れて実際に触ること。** 仕様のズレを見つける最初のチャンス。

---

## - [ ] T-13 応募者管理

**目的**: 掲載企業が応募を取りこぼさず選考を進められるようにする

**依存**: T-12

**作業内容**
- `application_status_logs` / `application_notes` マイグレーション
- 掲載企業の管理画面: 応募者一覧・詳細
  - ステータス・求人・事業所・応募日・流入元で絞り込み
  - **未対応(新規応募)の件数を目立たせる**
- 応募者詳細で応募時のスナップショット(履歴書)を表示
- 選考ステータスの変更([SPEC.md 8.3](SPEC.md#83-選考ステータス))と履歴記録
- 応募者へのメモ(社内向け・複数件)
- 運営者が全掲載企業の応募状況を横断確認できる画面
- CSV 出力(氏名・連絡先・応募日・ステータスのみ。監査ログに記録)

**完了条件**
- 応募者一覧が表示され、絞り込みが全条件で動く
- 応募者詳細で応募時の履歴書が読める
- ステータスを変更でき、変更履歴が全件記録される
- 不採用・辞退にした応募者も一覧に残る
- メモの追加・削除ができる
- CSV 出力が動作し、監査ログに記録される
- **他社の応募者に 404 が返るテストが通る**(一覧・詳細・ステータス変更・メモ・CSV の全てで)

---

## - [ ] T-14 アグリゲーション連携 ★主要な売り

**目的**: 掲載しただけで応募が来る状態を作る。**この製品の中核**

**依存**: T-10

**作業内容**
- **実装前に各媒体の最新の公式ドキュメントで XML フォーマットと必須項目を確認する**
- フィード出力エンドポイント
  - `/feed/indeed.xml`
  - `/feed/kyujinbox.xml`
  - `/feed/stanby.xml`
- Google しごと検索向けの JSON-LD は T-10 で実装済み。項目の充足を確認する
- **出力対象は `status = published` かつ `allow_external_feed = true` かつ掲載期間内の求人のみ**
- 掲載企業と運営者の双方が配信可否を制御(運営者の設定が優先)
- フィードは日次で静的ファイルとして生成し、キャッシュから配信する(毎回 DB を舐めない)
- **チャンク処理にする**(共有レンタルサーバーの実行時間・メモリ上限のため)
- 流入元の判別と `applications.referrer_source` への記録
- 運営者管理画面に媒体別の流入数・応募数を表示

**完了条件**
- 各媒体のフォーマットで妥当な XML が出力される
- **非公開・審査待ち・掲載終了・配信不可の求人が一切含まれない**
- 求人が 1,000 件あってもタイムアウトせずに生成できる
- 外部から流入した応募の流入元が記録される
- 運営者画面で媒体別の効果が見える
- 各媒体の仕様確認日と参照ドキュメントをコード内コメントに残している

---

## - [ ] T-15 メール通知

**目的**: 応募・審査の動きを関係者が確実に知る

**依存**: T-13, T-08

**作業内容**
- 外部 SMTP(SendGrid / SES)の設定
- **キューは cron 実行前提で設計する**(常駐プロセスが使えない)
  - ドライバは `database`
  - `php artisan queue:work --stop-when-empty --max-time=50` を毎分の cron で回す想定
  - **通知が最大1分遅れることを前提にした文言にする**(「すぐに届きます」と書かない)
- 通知の種類
  - 掲載企業へ: 新規応募、審査の承認・差戻し
  - 運営者へ: 新しい審査待ちが発生
  - 求職者へ: 応募を受け付けた、選考ステータスが変わった
- メールテンプレートにメディア名・ロゴを反映(`site_settings` から)
- ローカルでは mailpit で確認

**完了条件**
- 応募すると掲載企業と求職者の双方にメールが届く
- 審査の承認・差戻しが掲載企業に通知される
- **キューワーカーを常駐させず、`queue:work --stop-when-empty` の単発実行だけで送られる**
- キューワーカーが動いていなくても応募・審査自体は成功する
- 失敗したジョブが `failed_jobs` に記録され、再実行できる
- メール本文にメディア名が正しく入る

---

## - [ ] T-16 運営者管理画面の仕上げ

**目的**: 顧客が自分でメディアを運営できる状態にする

**依存**: T-09, T-13

**作業内容**
- ダッシュボード(審査待ち件数・掲載中求人数・今月の応募数・媒体別流入)
- サイト設定画面(メディア名・ロゴ・テーマ選択・連絡先・規約・審査ON/OFF・会員機能ON/OFF)
- マスタ管理画面(資格・施設形態・職種などの有効/無効と並び順)
- 監査ログの閲覧(操作者・期間・対象で絞り込み)
- 記録対象の実装漏れ確認: 審査の承認/差戻し、応募者情報の閲覧・CSV出力、プラン変更、サイト設定変更、データ削除

**完了条件**
- 運営者がサイト設定を管理画面から変更でき、即座に反映される
- マスタの有効/無効・並び順を変更でき、公開サイトと入力画面に反映される
- 上記の全操作が `audit_logs` に記録される
- 監査ログを絞り込んで閲覧できる
- `audit_logs` にインデックスが張られている

---

## - [ ] T-17 データ返還と完全削除

**目的**: 契約書に書く「解約時のデータ返還・完全削除」を実行できるようにする

**依存**: T-16

**作業内容**
- 全データのエクスポート(掲載企業・事業所・求人・応募・会員を CSV または JSON 一式)
- アップロード画像を含めた一括ダウンロード
- 完全削除機能(**二段階確認 + 監査ログへの記録**)
- 求職者の退会時のデータ削除(応募時スナップショットは選考記録として残る旨を規約に明記)
- 手順書 `docs/解約時のデータ返還手順.md`

**完了条件**
- 全データをエクスポートでき、中身が実データと一致する
- 画像を含めてダウンロードできる
- 完全削除が二段階確認を経て実行され、監査ログに残る
- 求職者が退会でき、個人情報が削除される
- **実際に手順書の通りに操作して、返還と削除が通ることを確認済み**

---

## - [ ] T-18 デプロイと全環境への配布

**目的**: 顧客が増えても更新を配れる体制を作る。**後回しにすると顧客 5 社で詰む**

**依存**: T-01

**作業内容**
- Xserver への設置構成([SPEC.md 13.2](SPEC.md#132-環境構成))
  - Laravel 本体を公開領域の外に置く
  - `public_html` を `app/public` へのシンボリックリンクに置き換える
  - `.env` が Web からアクセスできないことを URL を叩いて確認
- `deploy.sh` の作成
  - `git pull` → `composer install --no-dev --optimize-autoloader` → `migrate --force` → キャッシュ再構築
  - **`public/build` はローカルでビルドしたものを配布**(サーバでビルドしない)
- cron の登録(管理パネルから)
  - 毎分: `php artisan schedule:run`
  - 毎分: `php artisan queue:work --stop-when-empty --max-time=50`
- スケジューラに掲載期限切れ処理とフィード生成を登録
- バックアップ(Xserver の自動バックアップの確認 + 自前の日次 mysqldump を環境外へ退避)
- **環境台帳** `docs/環境台帳.md`(顧客名・ドメイン・サーバ情報・現在のバージョン・最終デプロイ日)
- 手順書 `docs/新規環境の構築手順.md` と `docs/デプロイ手順.md`

**完了条件**
- 本番 URL でサイトが表示され、HTTPS で接続される
- **`https://ドメイン/.env` にアクセスして 404 になる**
- `deploy.sh` を実行するだけでデプロイが完了する
- cron が動作している(掲載期限切れ処理のログで確認)
- **バックアップからのリストアを実際に試して成功している**
- `APP_DEBUG=false` になっている
- **手順書の通りに作業して、新しい環境を 1 つ構築できている**

---

## - [ ] T-19 デモメディアの立ち上げ

**目的**: 営業資料を作ると同時に、**自分がヘビーユーザーになって仕様のズレを見つける**

**依存**: T-14, T-18

**作業内容**
- デモ用の環境を 1 つ構築する(T-18 の手順書を実際に使う)
- 架空または実在の介護事業所のデータを投入する(実在なら許諾を得る)
- 掲載企業アカウントを作り、**実際にセルフ入稿から審査・公開まで通す**
- 求職者として会員登録し、実際に応募する
- アグリゲーション媒体への申請を出す
- 使ってみて気づいた問題を洗い出し、TASKS.md に追加タスクとして起票する
- 営業資料(画面キャプチャ・機能一覧・料金表)を作る

**完了条件**
- デモメディアが公開されており、URL を営業先に見せられる
- 求人が 30 件以上入っている
- セルフ入稿 → 審査 → 公開 → 応募 → 選考管理の一連が実データで通る
- **使って気づいた問題が TASKS.md に起票されている**(ゼロなら使い込みが足りない)
- 営業資料ができている

---

# Phase 2 — 拡張

契約が取れてから、あるいは商談で必要性が明らかになってから着手する。

## - [ ] T-20 スカウト機能

**依存**: T-13

**⚠ 着手前の必須確認**: 職業安定法上、特定募集情報等提供事業者の届出が必要になるかを確認する([SPEC.md 12.4](SPEC.md#124-職業安定法上の位置づけ))。これは顧客側の義務だが、導入支援の一環として案内できるようにする。

会員検索 / スカウト送信 / テンプレート / 送信履歴・開封状況 / **求職者の受信拒否設定(必須)** / 送信上限 / プレミアムプランのみ

## - [ ] T-21 CSV 一括登録

**依存**: T-07

求人・掲載企業の CSV 一括投入。既存メディアからの移行と、導入支援でのデータ投入に使う。チャンク処理必須。

## - [ ] T-22 NGワード検知・最低賃金チェック

**依存**: T-08

競合が標準搭載しており商談で比較される([SPEC.md 16.5](SPEC.md#165-競合との機能差))。審査画面での警告表示として実装する。**最低賃金は毎年改定されるため、マスタとして持ち更新できるようにする。**

## - [ ] T-23 お祝い金機能

**依存**: T-13

入社決定時のお祝い金の設定と支給管理。求職者の応募動機になる。

## - [ ] T-24 代理店階層

**依存**: T-05

運営元 / 代理店 / 掲載企業の 3 階層。**全画面の権限判定を見直すことになる**ため、必要性が確定してから着手する。

## - [ ] T-25 LINE 連携通知

**依存**: T-15

介護領域の求職者はメールを見ない層が相当いる。LINE 公式アカウント連携による通知。プレミアムプランのみ。
