# CLAUDE.md — 実装ルール

このリポジトリで作業するときに必ず従うルール。仕様は [SPEC.md](SPEC.md)、作業計画は [TASKS.md](TASKS.md) を参照する。

---

## 1. プロジェクト概要

**介護・医療・福祉に特化した求人メディア構築パッケージ。**
顧客(介護業界団体・人材会社・地域メディア)がこれを導入すると、複数の介護事業所の求人が掲載される求人メディアが立ち上がる。初期費用 50〜150万円 + 月額 5〜10万円で、導入支援とセットで販売する。

**顧客ごとに独立した環境(Xserver を 1 契約)へ設置する。1環境 = 1メディア。**

---

## 2. 技術スタック

| 領域 | 採用技術 |
|---|---|
| フレームワーク | **Laravel 13.8** |
| PHP | **8.4**(8.3 はセキュリティ修正が 2026年12月終了のため採用しない) |
| DB | MySQL 8.0。**1環境 = 1DB。テナント分離は不要** |
| 描画 | Blade によるサーバサイドレンダリング(公開側は SEO が生命線) |
| CSS | Tailwind CSS 4 / ビルドは Vite 8(Node 20+ が必要) |
| **本番環境** | **エックスサーバー(共有レンタルサーバー)** — Apache + `.htaccess` |
| ローカル開発 | Docker。**本番とは構成が違うことを常に意識する** |
| メール | 外部 SMTP(SendGrid / SES) |

---

## 3. 絶対ルール

破ると製品が保守不能になる、あるいは法的リスクが発生するルール。**例外なく守る。**

### 3.1 コードは 1 本。顧客固有の分岐を作らない

全顧客が同じコードを使う。顧客ごとの違いを吸収してよいのは次の 3 つだけ。

| 許される差 | 例 |
|---|---|
| **設定** | `site_settings` の値(メディア名・審査ON/OFF・会員機能ON/OFF) |
| **DB** | マスタの有効/無効・並び順、掲載プランの定義 |
| **テーマ** | 見た目(→ 3.2) |

**やってはいけないこと**

- `if ($customer === 'kaigo-kyokai')` のような顧客名による分岐
- 特定顧客のためだけのカラム・テーブル・ルート
- 環境ごとに違うコードを置く

「この顧客だけ〜したい」という要望が来たら、**設定で表現できないか**をまず考える。できないなら、それは全顧客に提供する機能として作るか、断るかのどちらか。

### 3.2 テーマには見た目だけを書く

テーマ(`resources/views/themes/{name}/`)に入れてよいのは Blade のマークアップ・CSS・画像だけ。

| 書いてよい | 書いてはいけない |
|---|---|
| HTML マークアップ | Eloquent クエリ・DB アクセス |
| `{{ $job->title }}` のような変数の出力 | 業務ロジック・金額計算・日付計算 |
| `@foreach` `@if` による表示の出し分け | 認可判定・権限チェック |
| Blade コンポーネントの呼び出し | ファイル操作・外部API呼び出し |

**表示に必要なデータは全てコアのコントローラが渡す。** テーマが自分でデータを取りに行った瞬間、コアの変更でテーマが壊れるようになり、全環境への一斉配布ができなくなる。

新しい画面を作るときは、まず `resources/views/core/` に置く。テーマはそこからの差分だけを持つ。

### 3.3 フォーム項目を動的に追加できる仕組みを作らない

応募フォーム・会員登録フォーム・求人入力フォームの項目は**システム固定**。
運営者や掲載企業が任意の項目を追加できる機能を作ってはいけない。

理由: 項目追加を許すと「健康状態」「病歴」「障害の有無」の欄が作られる。要配慮個人情報の取得を仕組みで防ぐための設計。

### 3.4 要配慮個人情報の項目を作らない

**健康状態・病歴・障害・治療歴・国籍・信条・社会的身分に関するカラムと入力欄を作らない。**
自由記述欄には「健康状態・病歴などの機微な情報は記入しないでください」の注意書きを添える。

「あったほうが便利そう」という理由でこれを緩めない。取得しないことで管理義務そのものを回避している。

### 3.5 審査を通っていない求人を公開面に出さない

`status = published` 以外の求人を、公開サイト・検索結果・アグリゲーションフィード・サイトマップに出してはいけない。

- 公開中の求人が編集されたら `pending`(審査待ち)に戻す
- 掲載期限が切れたら `closed` にする
- `expires_at` を過ぎた求人が公開され続けることがないようにする

### 3.6 応募時の履歴書はスナップショット

応募が発生した瞬間、その時点の履歴書内容を `application_resume_snapshots` に**コピー**する。
掲載企業の画面が表示するのはスナップショットであり、`job_seekers` を直接参照してはいけない。

本人が後からプロフィールを直しても、企業が見る選考資料は変わらないのが正しい挙動。

### 3.7 テナント分離のコードを書かない

前身のプロジェクトと違い、**この製品にテナントの概念はない**。環境が物理的に分かれているため、`tenant_id` カラム・Global Scope・`BelongsToTenant` トレイトは**作らない**。

DB の中に他社のデータは存在しない。存在しない前提で単純に書く。

---

## 4. 本番環境の制約

本番は共有レンタルサーバーであり、**Docker も常駐プロセスも Node.js も使えない**。ローカルで動いても本番で動かない実装をしないこと。

| 禁止 | 理由と代替 |
|---|---|
| **`supervisor` 前提のキュー実装** | 常駐プロセスが使えない。cron から `queue:work --stop-when-empty --max-time=50` を毎分実行する前提で設計する。**通知は最大1分遅れる**ことを許容した仕様にする |
| **本番での `npm run build` 前提** | サーバに Node.js がない。ビルドはローカルで行い `public/build` を配布する。`public/build` を `.gitignore` に入れない |
| **Docker のサービス名を使った接続** | 本番に `mysql` というホストは存在しない。接続先は必ず環境変数から読む |
| **1リクエストでの大量データ処理** | 実行時間・メモリに上限がある。フィード生成や一括処理は必ずチャンク処理にする |
| **Web サーバ設定の書き換え前提の機能** | Apache の設定は触れない。リダイレクト等はアプリ側か `.htaccess` で行う |
| **Redis 前提の実装** | キャッシュ・キュー・セッションのドライバは `file` / `database` を前提にする |
| **`composer.json` の `config.platform.php` を外す** | 本番の PHP バージョンを固定している。外すと開発機の PHP で依存が解決され、本番で動かないパッケージが入る |

詳細は [SPEC.md 13.1](SPEC.md#131-共有レンタルサーバーの制約と対処) を参照。

---

## 5. ディレクトリ構成と責務

```
app/
  Models/                  Eloquent モデル。リレーションとスコープのみ
  Http/
    Controllers/
      Public/              公開メディアサイト(求職者が見る画面)
      Company/             掲載企業の管理画面
      Admin/               運営者の管理画面
      Seeker/              求職者マイページ
      Feed/                アグリゲーション向けフィード出力
    Middleware/            プラン制限チェック・テーマ解決
    Requests/              バリデーション(Controller に書かない)
  Services/                業務ロジック。Controller から呼ぶ
  Support/                 テーマ解決などの共通機構
resources/views/
  core/                    コア標準テンプレート(全顧客共通のフォールバック)
    public/ company/ admin/ seeker/
  themes/
    default/               標準テーマ
    {顧客名}/               顧客専用テーマ(差分のみ)
public/themes/{顧客名}/     テーマの CSS・画像
tests/Feature/             機能テスト
docs/                      運用手順書(環境構築・デプロイ・テナント開設)
```

**責務の原則**

- Controller は「受け取る・呼ぶ・返す」だけ。業務ロジックを書かない
- 複数モデルにまたがる処理・トランザクションが必要な処理は `Services/` に置く
- バリデーションは必ず `Requests/` の FormRequest に書く
- モデルには業務ロジックを詰め込まない(リレーション・アクセサ・スコープまで)

---

## 6. 命名規約

| 対象 | 規約 | 例 |
|---|---|---|
| テーブル | スネークケース・複数形 | `job_postings`, `job_posting_reviews` |
| 中間テーブル | 単数形_単数形(アルファベット順) | `job_posting_qualification` |
| モデル | パスカルケース・単数形 | `JobPosting`, `PostingPlan` |
| Controller | 役割 + `Controller`。文脈別ディレクトリに置く | `Company/JobPostingController` |
| ルート名 | `文脈.リソース.アクション` | `company.job-postings.edit` |
| Blade | 文脈ディレクトリ + ケバブケース | `core/company/job-postings/edit.blade.php` |
| Service | 動詞 + 目的語 + `Service` | `SubmitJobPostingForReviewService` |

**業務用語は [SPEC.md 3](SPEC.md#3-用語定義) の用語定義に合わせる。**

- 掲載企業は `company`。求人メディアの運営者は `admin`
- 事業所・勤務地は `workplace`(`facility` は施設形態マスタ `facility_types` に使う)
- 求職者はモデル・テーブルで `job_seeker`、ルートとガード名のみ `seeker`

---

## 7. テスト方針

**主要機能は全て Feature テストを書く。** 実装だけしてテストを書いていない状態で「完了」と報告しない。

### 7.1 必ずテストする対象

| 対象 | 落ちると何が起きるか |
|---|---|
| **審査フロー** | 審査前の求人が公開される |
| **公開判定** | 掲載終了・期限切れの求人が出続ける |
| **掲載プランの制限** | 顧客の収益モデルが壊れる |
| **権限**(運営者/掲載企業/求職者) | 他社の応募者情報が見える |
| **応募時のスナップショット生成** | 選考の証跡が残らない |
| **アグリゲーションフィードの出力対象** | 非公開の求人が外部媒体に流れる |

### 7.2 権限テスト

掲載企業に属するリソースについて、次の形のテストを書く。

```php
/** @test */
public function 他社の求人にはアクセスできない(): void
{
    $companyA = Company::factory()->create();
    $companyB = Company::factory()->create();
    $userA    = CompanyUser::factory()->for($companyA)->create();
    $postingB = JobPosting::factory()->for($companyB)->create();

    $this->actingAs($userA, 'company')
         ->get(route('company.job-postings.edit', $postingB))
         ->assertNotFound();   // 403 ではなく 404(存在自体を知らせない)
}
```

**403 ではなく 404 を返す。** 403 は「そこに何かがある」ことを教えてしまう。

### 7.3 テーマのテスト

- テーマに該当ファイルがあればテーマが使われ、なければコアにフォールバックすること
- テーマを切り替えても全ページが 200 で表示されること

### 7.4 実行

```bash
php artisan test                          # 全テスト
php artisan test tests/Feature/Admin      # 運営者画面のみ
php artisan test --filter=Review          # 審査フローのみ
```

---

## 8. 作業の進め方

1. **[TASKS.md](TASKS.md) の順に進める。** タスクの「完了条件」を全て満たしてから次へ移る
2. 実装前に SPEC.md の該当セクションを読む。仕様と食い違う実装をしない
3. 仕様に書かれていない判断が必要になったら、勝手に決めずに確認する。ただし明らかな定型処理(バリデーションメッセージの文言など)は進めてよい
4. 仕様の変更が決まったら、**コードと同じコミットで SPEC.md を更新する**
5. 1 タスク完了ごとにコミットする。コミットメッセージは日本語でよい

### 8.1 やってはいけない進め方

- テストを後回しにして機能だけ積み上げる
- 「この顧客だけ」の分岐をコードに入れる
- テーマにクエリやロジックを書く
- 「とりあえず動くから」で公開判定を素通しにする
- SPEC.md を更新せずに仕様を変える

---

## 9. ドキュメント

**ドキュメント(README・設計書・仕様書・運用手順書・レポート)は日本語で書く。**
コード中のコメントも日本語でよい。変数名・関数名は英語。

運用手順書(環境構築・デプロイ・顧客への導入手順)は `docs/` に置く。
**顧客数が増えるほど手順書の価値が上がる。** 実際に手順通りに作業して通ることを確認してから完了とする。
